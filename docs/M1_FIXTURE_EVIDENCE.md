# M1 场景夹具复测记录

日期：2026-09-04。SC02 IN_PROGRESS；本记录只证明合成测试窗口中的具体像素与场景行为，不证明真实应用输入、双软件或耐久验收。

## 夹具与失败修正

`tools/Workbench.DesktopFixture` 已在前次提交加入，包含自动场景测试及交互 F0 窗口。本次从提交版本复测，得到真实失败：

- `artifacts/verification/scene-fixture-20260904-091558-9438133/`：根窗和 Alpha 检查通过；`WindowFromPoint` 检查发现无关遮挡窗并未覆盖参考点，因此停止。该报告没有记录实际命中句柄，不能据此认定是哪一个外部窗口遮挡。
- 修正只将本夹具自身的临时洋红遮挡窗设为 TopMost，不移动/隐藏用户其他窗口；报告新增期望/实际命中句柄和参考点。不取消实际遮挡检查，也不放宽像素容差。
- 修正后第一次 40 轮通过，随后加入输入核心并构建后再次对最终二进制做 40 轮验证。历史失败报告保留。

## 最终运行

命令 `./scripts/verify-scene.ps1 -Cycles 40`。

证据目录：`artifacts/verification/scene-fixture-20260904-092537-1869175/`，含 report.json、source-identity.json 和夹具 PNG。

- 87 项检查 PASS：7 个场景阶段及 40×2 开关检查。
- 真实 WGC → GPU 合成 → NV12 sample 创建；诊断阶段显式 CPU 读回合成 BGRA 做颜色比对。不是正常媒体路径每帧读回，也没有浏览器解码参与本套件。
- 透明/半透明/不透明红、蓝色根窗、遮挡后新绘制的绿色标记、同 PID 无关窗排除、根窗外多级 owner、真实模态禁用/恢复、静止主窗上的菜单消失均按独立期望检查。
- 实际遮挡命中与测试遮挡窗句柄相等；源释放后活动捕获计数为 0，累计绑定 44，最终 sceneVersion 为 86。
- Fixture DLL：`80C37768011C0D7B442518C0C7A4457AF5D8168FA6FC87C684597CAAB29F199C`。
- Windows DLL：`45DA86DACDB4D6D772BE3BF0238F7CED3A540A0AFBF70A907B70D49145158806`。

## 资源观察，不判耐久通过

最终运行约 11 秒，进程初始句柄 608、循环前 646、最后一轮 694、源 Dispose 后 674；GDI 22→24，USER 30→53→51→50；初始工作集约 145 MiB，Dispose 后约 226 MiB。最后采样发生在测试主窗关闭前，包含诊断读回、托管/原生缓存及测试 UI。

这些数据说明项目跟踪的活动捕获节点清零，但**没有证明所有原生资源完全回到初始值**。其他短时运行的回落幅度也不同，不能据此断言泄漏或无泄漏。SC02 资源长时间/隔离对照仍待做，P09/P10/P13 未通过；不得用重复的短跑替代八小时验收。

## 前次 F0 浏览器报告复核

本次只读核对 `artifacts/verification/media-probe/run-20260904-091020-4104978.json`。该运行的 Windows DLL 为历史 `AFE67DC0ABB20B454A3A1F2FB84EE65B6C4E400FA96CAD823ABDF19DB68144EB`，不是上面的最终构建。

报告显示 Intel H264、1280×720、60.016 秒，输入/编码/接收/解码/呈现各 3 帧，浏览器 PASS；场景为 3 节点→1 节点，3 个节点对应 F0 主窗和两级菜单。没有模态编辑框场景，不能把当时工具稍后打开的模态框算进这个一分钟媒体证据。配置 fps=30 不表示静止源每秒实际呈现 30 帧。

F0 的文字和菜单此前由 Computer Use 工具操作，不是项目网页输入。当前测试服务和夹具已停止；没有等待中的原生测试进程。

## 续接

继续 [M1-02 输入核心记录](./M1_INPUT_CORE_EVIDENCE.md) 的原生后端与浏览器接入，补 NX 参数框和真实工作流。场景夹具保留作为回归，不再重复重建。最终用户验收仍按原 A/P 矩阵执行。
