# 项目实施状态

最后更新：2026-09-04

当前状态：**M0/M1 实施中，按用户要求保存 Git 检查点，等待明天继续；正式远控未完成**。已有输入安全核心及受控原生后端，自身测试窗口 19 项真实输入检查通过；最近回归为 108 项 C# 和 5 项 JavaScript。历史场景夹具 87 项及真实 NX 菜单/文件框媒体证据保留。网页输入链、NX 参数框、真实动态持续测试与耐久仍缺，不能据此放行 M1。

完整实施目标保持 active。本轮从现有状态续接，没有重复创建目标。上一轮 Git 提交/推送与交接属于 progress；本轮新增输入核心、修正夹具失败并产出新证据，也属于 progress。尚未满足全项目完成定义。

## 已确认的产品约定

- 单人通过另一台电脑的浏览器，远程操控本机 NX / TIA Portal。
- UG 绘图区与完整界面可切换，保留建模编辑和原生菜单/快捷键。
- 使用被控电脑本地文件及原生文件对话框。
- 软件单开满屏、双开网页左右等宽。
- 公网穿透由用户配置 Cloudflare；锁屏及登录前控制暂不实现。
- 目标为持续实际工作，不能按展示 Demo 的标准交付。
- 独立 Web 项目、桌面 Chrome/Edge、现有显示环境是当前设计默认值。

## 阶段进度

| 阶段 | 状态 | 现有证据 | 下一步 |
| --- | --- | --- | --- |
| 设计 | DONE | 主规划书 1.2、实现契约/依赖矩阵、SC01–SC08、验收、目标正文与资料 | 用户审阅，后续按当前状态实施 |
| M0 环境与基线 | IN_PROGRESS | 基线一致；doctor v2 覆盖双显示器/DPI/工具实际版本/快捷方式；现有项目 locked restore 通过 | 补测试样本与 NX/TIA 实际可编辑能力；完整前端锁定后续完成 |
| M1 技术探针 | IN_PROGRESS | 真实 NX 历史媒体证据；历史场景夹具 87 检查；自身窗口原生输入 19 检查；108 C# / 5 JS 回归 | 有界输入执行器、捕获身份绑定/网页联调、真实 3D 运动/参数框、资源隔离验证、JPEG/双路及 1080p |
| M2 骨架/鉴权/协议 | NOT_STARTED | 无 | M1 门槛通过后实施 |
| M3 应用/输入 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M4 布局/裁剪/弹窗 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M5 媒体/性能 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M6 恢复/异常 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M7 真实验收 | NOT_STARTED | 无 | 不能由模拟测试替代 |
| M8 发布交付 | NOT_STARTED | 无 | 形成源码、发布包和验收报告 |

阶段状态为 NOT_STARTED、IN_PROGRESS、DONE、WAITING_EXTERNAL 或 FAILED。DONE 需要实际证据；文档内允许状态不等于平台目标工具的状态，更新工具时遵循工具本身规则。

### 已有文件的具体判定

| 任务 | 状态 | 证据与尚缺内容 |
| --- | --- | --- |
| M0-01 | DONE | legacy/demo-v3 三文件与根目录 SHA-256 一致；已有忽略规则；原文件未修改 |
| M0-02 | DONE | doctor v2、新原生显示枚举已运行；environment-20260903-m0-v2.json 包含 OS/GPU/两输出/DPI/实际 SDK/软件版本和安装快捷方式目标；不输出许可与完整参数 |
| M0-03 | IN_PROGRESS | .NET 10.0.400、Node v24.20.0；现有五项目 locked restore/build 已通过；正式 React 前端及全量构建依赖未完成 |
| M0-04 | IN_PROGRESS | 历史只读 070.prt 可见；本次 NX 为空白工作区，新建框因工具焦点/控件索引问题取消；创建/编辑/保存仍未验证；TIA 条件 WAITING_EXTERNAL |
| M1-01 | IN_PROGRESS | 历史 NX 同流菜单/文件框；当前合成夹具、受控 Alpha/遮挡/静止/模态检查通过；仍缺真实视图运动、参数框与资源耐久 |
| M1-02 | IN_PROGRESS | 已有 F0、InputSession/命令/坐标核心和原生后端，自身窗口 19 项真实输入检查通过；仍缺有界执行器、生产捕获身份绑定、网页输入链和真实应用完整键鼠验收 |
| M1-03 | IN_PROGRESS | 新增多节点 Alpha 合成和 sample/chunk 场景版本账本；真实窗口短时同流、取消重连通过；测试图历史十分钟通过。仍缺真实动态持续实验、JPEG、1080p/双路，不满足 M1 门槛 |
| M1-04 | IN_PROGRESS | NX 菜单/原生文件框局部媒体证据已具备；不是 NX+TIA 双应用，尚无完整输入、编辑弹窗与双窗口验收 |

源代码、截图与诊断报告均属局部技术证据。M1 不因截图或编码器 ActivateObject 成功而 DONE。当前仍无 Host、正式 Agent、浏览器控制台或发布包。

SC 子检查当前状态：SC01 DONE（既定纯逻辑关联/几何范围）；SC02、SC03、SC04 IN_PROGRESS；SC05–SC08 NOT_STARTED（产品输入尚未接通）。历史范围见 [M1_SCENE_EVIDENCE.md](./M1_SCENE_EVIDENCE.md)，新增范围见 [场景夹具](./M1_FIXTURE_EVIDENCE.md) 和 [输入核心](./M1_INPUT_CORE_EVIDENCE.md)。不得因一个 SC 项完成提前进入正式 M2。

## 已知外部依赖与风险

| 编号 | 条件 | 当前信息 | 处理方式 |
| --- | --- | --- | --- |
| E01 | NX 可采集/可编辑 | 已采到真实模型首帧；尚无完整建模/输入/保存报告 | 继续 M1，再用独立临时模型验收 |
| E02 | .NET 10 SDK | 项目本地 10.0.400 版本命令成功；已不缺 SDK 本体 | 验证完整锁定构建；不重复下载安装 |
| E03 | H.264 硬件编码 | Intel Quick Sync 已接真实 WGC/D3D 源并在本机浏览器短时显示；测试图历史十分钟通过 | 继续真实动态场景、1080p、双路和正式质量/恢复测试 |
| E04 | 博图安装与许可证 | 本次未验证有可用实例 | 根据所需组件准备 V21 实际环境 |
| E05 | 博图系统匹配 | 本机 Home 25H2；官方所列 Home 24H2 仅基础版 | 核对组件、最新兼容说明和支持环境，不擅自改 OS |
| E06 | 真实工程副本 | 未提供代表性样本 | 先临时夹具/简单模型，后加入真实副本 |
| E07 | 第二台客户端 | 未验证 | 后续 LAN 实测 |
| E08 | CF 入口 | 用户自管，未提供域名 | 先交付代理兼容，后续公网验证 |
| E09 | 工具原生桌面操作能力 | Computer Use 已恢复并观察 NX；尚无完整输入工作流 | 用工具辅助真实验证；工具画面不能冒充项目采集/输入证据 |
| E10 | CF 公网持续画面流的使用条件 | 官方 FAQ 明确公网视频/大文件有服务条件；本项目交互流具体适用性未确认 | 用户结合服务方案/服务方确认；之前继续 LAN 与本地代理测试，不替用户购买或改网络架构 |

这些不是设计阶段的阻塞；不得因此把实施任务擅自标记完成。

## 实施循环记录模板

每个连贯任务追加一条：

```text
日期/时间：
任务编号：
状态：
本次修改文件：
验证命令或真实操作：
实际结果：
证据路径：
剩余风险/外部依赖：
下一项：
设计变更（若有）：
```

## 本轮设计复核记录

- 任务：设计 1.1。
- 修改：主规划书、实现契约（新增）、验收标准、目标正文、资料依据、实施状态。
- 检查：PPT 第 5–7 页全文；Demo 静态源码与副本哈希；项目工具版本；Windows/CF/WebCodecs/Siemens/OpenAI 官方资料。
- 设计修订：Host/Agent 故障双向释放、静止画面与断流区分、跨通道代次、输入/场景事务、禁止异机不安全 HTTP 远控、CF 公网服务条件、36 项任务依赖。
- 证据边界：仅文档和只读检查通过，不增加真实功能 PASS 项。

## 2026-09-03 15:16 实施记录

- 任务：M0-02、M1-01、M1-03（后两者仅局部进展）。
- 源码：修复 D3D11 公共重载；新增 EncoderCatalog、DisplayCatalog；补窗口身份/可见子节点/客户区；探针提供安全报告输出、取消、尺寸变化失败提示；新增 verify-probe.ps1，补全 doctor v2。
- 验证：`.\scripts\verify-probe.ps1 -CaptureNx` 两轮通过；最新 `artifacts/verification/probe-20260903-151616-2305538/report.json`，9 项 PASS；构建 0 错误/0 警告。该套件是冒烟测试，不是 34 项验收矩阵。
- 原生证据：`m1-wgc-nx-first.png/.json`；`m1-encoder-activation.json`；`environment-20260903-m0-v2.json`；详见 M1_INITIAL_EVIDENCE.md。
- 保护：原 Demo 三个 SHA-256 仍一致；NX 原模型只观察、未保存/修改；没有改变显示/许可/CF 配置。
- 风险：显示输出 DPI 与旧 NX 窗口 DPI 不同，不能假定单比例；第二次采集期间前台/模型位置变化，未当成受控遮挡通过；硬件激活仍不等于编码能力。
- 下一步：M1-03 实际 H264 输入/输出和浏览器最小解码链，准备 Windows 测试夹具；在可控桌面时段补 NX 菜单/弹窗、动态和输入验证。

当前没有后台测试进程待轮询。续接时读最新报告并检查源码，从上述下一步继续；不重做 M0-01，不重新安装工具链，不把 TIA/第二客户端/CF 待验证项写成通过。

## 2026-09-03 设计规划书交付校订

- 本轮响应“先出具完整规划书”的要求，只复核文档与现有证据，未修改业务源码、操作 NX/TIA 或新增实施目标；已有完整目标状态未改动。
- 主规划书与 REFERENCES 更新了双显示器/DPI、已构建探针、真实首帧及编码器激活的证据边界，修正落后于 15:16 报告的文字。
- 规划仍为 1.1；36 项任务、34 项功能/安全验收、13 项性能/稳定性目标与协议契约不变。设计交付不改变 M0/M1 的 IN_PROGRESS 状态，不新增功能 PASS。
- 后续编码入口：GOAL_PROMPT 的推荐目标正文或已有目标续接正文，必须连同 IMPLEMENTATION_CONTRACTS 读取；先完成当前 M1 缺项，再按依赖推进正式功能。

## 2026-09-03 M1-03 实际编码与浏览器链路

- 上一目标轮属于 progress（校正权威设计/证据状态）；本轮新增实际源码与真实编码/解码证据，仍为 progress。目标继续 active，未缩小范围。
- 新增 H264Probe、AnnexBAccessUnits、Workbench.MediaProbe、20 项单元测试及媒体/HTTP/资源脚本；扩展 CLI，保持原 Demo 不变。
- 真实 Intel 1280×720 编码经本机 WS/WebCodecs 连续十分钟，18000 帧全量解码，601 次动态像素检查零错误；源码边界保护更新后的版本另做短时复测。
- 验证路径、构建哈希、原生错误修复、采样范围与局限见 [M1_MEDIA_EVIDENCE.md](./M1_MEDIA_EVIDENCE.md)。P01–P13 没有因此被整体标记通过；尤其不是输入时延、真实工程工作流或八小时证据。
- `verify-media.ps1`：20 项逻辑测试、硬件/软件各 90 帧、无效参数不产物与禁止覆盖通过。`verify-probe.ps1`：四项目 locked restore/build，0 警告/0 错误，8 项冒烟通过（未碰 NX）。
- 取消后 busy=false，同一个服务进程可重新申请编码。停止按钮不是产品租约/故障安全释放的实现，因为此工具不注入输入。
- 下一步：将实际 WGC/D3D 纹理接入该编码路径，补 GPU 转换与 Windows 输入夹具，再回到真实 NX 菜单/弹窗/建模和 M1-04；保持 TIA/第二客户端/CF 等待外部环境的状态。

15:54 收尾：最终版本取消后同进程一分钟重连通过（1800 帧、61 次像素检查、0 错误），报告 `media-probe/run-20260903-155200-3274576.json`。HTTP 四项复测通过。测试服务和资源采样均已结束，临时浏览器页已关闭，没有后台测试等待续接；无需重新运行已经通过的测试图十分钟试验来代替真实 WGC 源接入。

## 2026-09-03 完整规划书交付复核

- 响应本次“先出具完整任务规划书、后续采用目标方式实现”的请求，当前交付为文档复核，不继续原生 UI 操作或业务编码，不新建/完成目标。
- 已再次读取 PPT 实际第 5–7 页完整文字、原 Demo、主规划书、实现契约、验收与目标正文；Demo 三文件与 legacy 副本 SHA-256 仍一致。
- 修正主书第 2.1 节落后于媒体报告的表述，新增第 14.1 节实施续接检查点；36 项任务和 A01–A34、P01–P13 不变。
- 工作树已有尚未形成真实运行证据的 `WgcNv12Source.cs`、`IProbeFrameSource.cs`、H264 `sourceFactory` 接口及 CLI 临时 `--inspect-graphics-api` 分支。本次只确认源码存在，不把此前测试图构建的通过结果归到这些增量，也未在本次复核中测试它们。
- 下一实施步骤：完成真实源入口、颜色与资源生命周期检查，锁定构建并回归原路径，取得真实 NX → WGC/D3D → H264 → 浏览器证据，再补输入/弹窗与 M1 门槛。原生环境届时重新枚举，不沿用历史 HWND 或最小化状态。
- 外部条件仍为 TIA 安装/组件/授权及系统匹配、典型工程副本、第二客户端和 CF 环境/使用条件。它们不阻止当前设计交付；不得因此把完整实施目标标为完成。

## 2026-09-03 M1 真实 WGC/GPU 源实施

- 上一轮分类为 progress（规划与真实源码续接点已校正）；本轮实现真实 GPU 源入口、低延迟与色彩配置，并形成实际浏览器证据，仍为 progress。完整目标继续 active。
- 主要文件：WgcNv12Source、IProbeFrameSource、GraphicsCaptureInterop、H264Probe、两个 Probe 入口、媒体页面/说明、回归脚本和单元测试。临时 API 反射分支已移除。
- 真实结果：NX 源 10 秒及 60 秒本机浏览器测试，分别 1 帧及 7 帧全量解码；静止源按变化出帧，不写成 30 FPS。MF 低延迟属性使静止首个编码输出无需等待排空。
- 发现并保留缺口：NX 文件菜单为独立 owned HWND，主流不含菜单；单独 WGC 截图和 GPU 编码成功。A15 尚未通过，不把主窗口媒体 PASS 视为完整支持。
- 回归：23 项测试、硬件/软件各 90 帧、8 项构建冒烟、8 项 HTTP/真实 WS 握手拒绝检查通过。最终构建真实源取消后同进程重连成功。完整路径与二进制身份见 [M1_WGC_GPU_EVIDENCE.md](./M1_WGC_GPU_EVIDENCE.md)。
- 保护：仅恢复 NX、展开/收起文件菜单作观察，没有建模、保存或关闭工程，没有新增公网入口。工具对 NX 的原生操作不能当作项目输入链证据。
- 下一项：关联窗口场景捕获/合成与 Windows 夹具，进而完整输入、NX 文件框/编辑参数框、双窗口与 JPEG。M2 正式服务仍等 NX 技术门槛，不重跑测试图十分钟替代这些缺项。

16:28 收尾：最终构建真实源取消后重连及测试图 300 帧 / 11 次像素检查通过，完整文件名与哈希见本轮证据文档。测试服务及相关进程均已结束，临时页面已关闭；没有后台实验需要等待。NX 恢复为可见窗口，菜单收起，未修改/保存模型。下轮直接检查现有源码并推进关联窗口场景，不重复设计交付。

## 2026-09-03 规划书 1.2 交付

- 响应当前“先出具完整任务规划书、后续再实施”请求，只修改六份设计/状态文档；没有继续业务编码或原生 UI 操作。已有完整实施目标仍 active，没有新建、完成或修改目标范围。
- 核对：源 PPT 第 5–7 页完整文本；Demo 与 legacy 三文件哈希；真实源证据记录、菜单窗口快照、当前固定场景版本代码；Microsoft/Siemens/CF/W3C/OpenAI 官方资料。
- 修改：主书第 2.1/15.1 节更新真实源证据；新增 6.3/15.2；契约补充 4.4 与 SC01–SC08；同步验收子场景、版本和后续目标正文。详见主书设计变更记录。
- 设计影响：关联窗口准入、真实边界、透明混合、节点资源及异步编解码的场景标签不能留给后续代理猜测。36 个主任务、34 个 A 项和 13 个 P 项不减少。
- 验证：主书 36 项任务与契约 36 行依赖一一对应，无重复或差异；A 项 34、P 项 13、SC 子项 8；21 个文档本地链接无缺失；Demo 原件与副本的六次哈希检查全部一致。这是文档一致性检查，不增加功能 PASS；M1 仍 IN_PROGRESS，M2 以后不提前放行。
- 下一实施入口：现有源码与最新状态核对后按 SC01 起推进，后续完成项以实际记录为准；无须重复生成整份规划、解压或下载工具链。

## 2026-09-03 M1 关联场景实施

- 上一轮完成设计 1.2 复核；本轮依据现有完整实施目标推进 SC01–SC04，分类为 progress。没有重新创建目标，完整目标仍 active；未改变 36 项任务或 A/P 验收门槛。
- 新增 OwnedWindowScene、WgcOwnedNv12Source、GpuSceneCompositor、FrameSceneLedger 和浏览器 scene-timeline；扩展窗口元数据、CLI/媒体入口、回归脚本及测试。
- 真实失败驱动修正：不透明实验在 NX 菜单 Layered 阶段失败，随后实现预乘 Alpha GPU 混合并通过真实显卡像素校验；未静默丢菜单或退到整桌面采集。
- 同流真实证据：一分钟 NX 文件菜单 8 帧，节点 1→2→2→1；打开文件框静止 1 帧/2 节点；取消文件框一分钟 17 帧，节点 2→1、主窗恢复 Enabled。浏览器均实际解码和视觉核对。工具操作不是项目键鼠输入，未加载新文件或编辑/保存原模型。
- 最终回归：44 项 C#、5 项 JS、硬件/软件各 90 帧、8 项构建冒烟、8 项 HTTP/WS 边界全部通过。四项目 locked restore/build 0 警告/0 错误。所有报告路径、二进制身份、失败观察和局限见 [M1_SCENE_EVIDENCE.md](./M1_SCENE_EVIDENCE.md)。
- 取消后 busy=false，同一服务实例重连真实源 10 秒成功；最终构建默认测试图 300 帧全量解码、11 次像素检查 0 错误。没有把这些结果写成输入释放、丢包恢复或真实工程持续操作通过。
- 下一项：Windows 场景夹具、受控透明/遮挡/资源对照和 NX 编辑参数框，然后完整产品输入、双窗/JPEG、真实十分钟动态与 1080p，形成 M1 放行报告。TIA/典型工程/第二客户端/CF 条件继续按外部依赖表处理。
- 原 Demo 与 legacy 三份 SHA-256 全部仍一致；PPT 未改。所有临时媒体服务均已停止，临时浏览器页已关闭。NX 文件菜单/文件框已收起，原只读工程保留；续接无需等待历史后台测试。

## 2026-09-03 Git 交接检查点

- 按用户要求暂停功能开发，准备提交到 `https://github.com/XiaoyeCodes/TwinDesk`，明天继续。远端现有 `main` 初始化提交及 LICENSE 保留，不强制推送、不改写历史。
- 本次续接仅检查了状态、实现契约、项目配置和 Git 状态；用户打断前没有新增 Windows 场景夹具源码，也没有启动测试进程。不要把已宣布的下一步当作已经实现。
- 提交范围为源码、锁文件、脚本、设计/验收/状态文档和原 Demo。`.tools`、`artifacts`、构建目录、安装包、截图和本机敏感配置不进 Git。已有实测报告留在当前电脑，仓库里的报告链接不代表新电脑已具备这些本地文件。
- 明天优先读取本文件及 M1_SCENE_EVIDENCE，从 SC02 Windows 场景夹具/受控透明和遮挡验证继续，再补 NX 编辑参数框与产品输入。SC01 不重做；M1 未放行，完整项目未完成。
- 换电脑克隆后的工具准备与构建说明见 [REPOSITORY.md](./REPOSITORY.md)。不得为复现而自动安装/修改 Siemens 许可证或操作真实 PLC。

## 2026-09-04 Git 暂停与续接检查点

- 按用户“先提交推送，明天继续”的要求停止实施，把现有 Windows 场景夹具和 F0 交互测试目标作为阶段性增量保存。不代表 M1 放行或完整项目完成。
- 本次提交新增 `Workbench.DesktopFixture`、`verify-scene.ps1`，并为场景源增加仅供夹具调用的诊断像素读回及捕获节点计数。正常媒体路径不调用该读回方法；F0 只接收自身窗口事件，尚未实现产品远程输入。
- 提交前验证：五项目构建成功，0 警告、0 错误；44 项 C# 单元测试全部通过。当前没有重新进行真实 NX/TIA 或完整媒体端到端验收。
- 本机已有场景报告 `artifacts/verification/scene-fixture-20260904-085917-6301863/report.json`：40 轮窗口开关、87 项检查 PASS。该报告产生于添加交互 F0 窗口之前，不应归为当前最终夹具二进制的重新验证；资源数据仅为短时观察，不代表长时间无泄漏。
- F0 媒体观察报告位于 `artifacts/verification/media-probe/run-20260904-091020-4104978.json`，续接时应完整核对再作结论；本次交接不据此新增输入、模态操作或 NX 工作流 PASS。相关临时媒体服务和夹具进程已停止。
- 明天先读本检查点、实现契约和夹具 README，复测最终夹具版本，核对 F0 媒体结果，再推进真实 NX 编辑参数框与产品输入链。操作原生软件前重新枚举窗口，不复用历史句柄；M1 未通过前不提前放行后续正式服务。
- `.tools`、`artifacts`、构建输出、本机配置继续忽略，不上传安装包或运行截图。保留远端已有历史，使用普通推送。

## 2026-09-04 M1-02 输入核心与夹具复测

- 接到明确目标续接后，在干净的 `7022827` 上建立本地 `codex/m1-nx-input` 分支；本轮未提交或推送。上一 Git 交接轮和本轮均属 progress，完整目标保持 active。
- 场景夹具首次复测因“测试遮挡窗未实际覆盖参考点”失败，保留报告；只把自身临时遮挡窗置顶并增加实际命中诊断后，最终构建 40 轮共 87 检查通过。活动捕获归零，但原生句柄/内存未完全回初始值，长时资源验证仍未完成。
- 新增 InputCommand、PhysicalKeyMap、SceneInputCoordinates、InputSession、两份测试和 verify-input 脚本。实现仅 L0 输入安全核心，原生后端/输入 WS/浏览器事件尚未连接；SC05 仍 NOT_STARTED，不提前进入正式 M2。
- 最终证据：`input-core-20260904-092509-0089998` 定向 49 项（47 新增+2 既有）；`media-20260904-092624-3928431` 全量 91 C#、5 JS、硬件/软件编码各 90 帧；`probe-20260904-092512-5193409` 五项目构建与 8 冒烟；`scene-fixture-20260904-092537-1869175` 场景 87 检查。均在本机 artifacts/verification，来源哈希见报告。
- Computer Use 观察 NX 空白工作区及新建框；控件索引失效和焦点不可靠后取消，没有创建/修改/保存模型。N0 临时目录仍空；不能把新建框当作特征编辑框。上一 F0 一分钟报告只含菜单 3→1 节点、3 帧，未记录模态编辑框，不新增该项 PASS。
- 详细实现、失败和边界见 [输入核心](./M1_INPUT_CORE_EVIDENCE.md) 与 [场景夹具](./M1_FIXTURE_EVIDENCE.md)。原 Demo 与 legacy 六次哈希一致，PPT 未改；无本轮遗留测试进程。
- 下一步直接实现 M1-02 受控原生后端、输入环境检查、实际部分提交/Unicode 抬键与有界执行器，再接 F0 浏览器输入。SC04 真实参数框、双窗/JPEG、真实十分钟与 1080p、Host/Agent 故障保护、真实 TIA/第二客户端/耐久仍缺，不缩小完成条件。

## 2026-09-04 原生输入增量与 Git 交接

- 按用户最新要求停止功能开发，将当前 `codex/m1-nx-input` 增量提交并普通推送到远端 `main`，保留原有历史。当前段为续接检查点，不是完整目标完成声明。
- 新增 NativeInputBackend、NativeInputEvents、WindowsInputEnvironment、原生自测及回归测试；实际 SendInput 只在明确启动的自身测试窗口中验证，没有操作 NX/TIA。证据边界见 [M1_NATIVE_INPUT_EVIDENCE.md](./M1_NATIVE_INPUT_EVIDENCE.md)。
- 已落盘的最终报告：`native-input-20260904-093953-0351261` 为 19 项 PASS；`media-20260904-094147-9458926` 为 108 C#、5 JS、硬件/软件各 90 帧及边界检查通过；`probe-20260904-094050-6755895` 为构建与 8 项冒烟通过。均位于本机 `artifacts/verification`，本次交接只核对已有报告，不重跑原生软件测试。
- 早期心跳释放实测 6062 毫秒超过 6 秒目标，已保留失败边界说明；内部阈值改为 5.5 秒，最终自身窗口实测 5546.244 毫秒。单次受控结果不代表完整 P07 或真实网络/进程故障矩阵通过。
- 明天从有界输入执行器、真实捕获窗口生命周期绑定及 F0 回环浏览器输入联调继续，再推进 NX 参数框；不能用恒真身份校验回调绕过绑定验证。SC05–SC08、正式 M2 状态不提前更改。
- 原 Demo、PPT、许可证和公网配置不修改；构建输出、测试产物、安装包和本机凭据不上传。历史记录中的“尚无原生后端”仅描述当时状态，以本检查点为准。
