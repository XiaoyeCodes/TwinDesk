# 项目实施状态

> **范围变更（用户确认，2026-09-04）：本版仅要求同一台电脑通过浏览器控制真实 NX / TIA。第二台客户端、LAN 访问、公网/CF 及代理兼容不再是本版交付门槛，旧文中对应远程专属要求由本条覆盖，不能计为 PASS。真实建模/工程操作、菜单/弹窗/中文/保存、单应用与双应用布局、输入安全、恢复、诊断、8 小时耐久、打包及回滚要求继续有效。本机双屏是当前实验实现条件，不代表用户新确认了双显示器产品限制。主技术路线尚未变更；详见 PROJECT_STATUS.md 最新范围记录。**

最后更新：2026-09-04

当前状态：**M0/M1 持续实施中；正式本机控制产品未完成**。M0-03的前端锁定安装/类型检查/构建已通过。SC02有限GPU场景探针DONE：ALPC增量实测约130秒后回收，完整180次应用重建（201秒、循环内无强制GC）资源两次自行回落；不再把短时残留直接判为持续泄漏。SC03既有有限范围DONE；共享捕获版真实NX的SC04菜单/文件框/参数框同流已完成（74帧）；SC05有限最小输入链DONE：真实NX键鼠/中文/参数预览取消，F0双DPI、模态与控制断连松键已有实测。SC06有限NX/F0双路硬编/JPEG已完成。完整工作流、真实持续动态与8小时均未通过；异机要求已被用户取消，M1仍不放行。详见 [共享捕获](./M1_SHARED_CAPTURE_EVIDENCE.md)、[资源隔离](./M1_RESOURCE_ISOLATION.md)、[竞态](./M1_TRANSIENT_WINDOW_EVIDENCE.md) 与 [真实模型](./M1_NX_MODEL_EVIDENCE.md)。

完整实施目标保持 active。本轮从现有状态续接，没有重复创建目标。上一轮 Git 提交/推送与交接属于 progress；本轮新增输入核心、修正夹具失败并产出新证据，也属于 progress。尚未满足全项目完成定义。


## 当前续接入口（LOCAL-1，2026-09-04）

下一项 **LC-01 补齐实际分段/可见响应基线，复测 LC-02/03 队列候选，再据 nativeChecks 耗时推进 LC-04**。用户本轮明确要求先借鉴 Sunshine/noVNC 改进输入队列，并独立准备 Sunshine + moonlight-web-stream 对照；已实现并验证公共部分。任务顺序、依赖、验证和原验收映射见 [本机任务计划](./LOCAL_IMPLEMENTATION_PLAN.md)；换电脑先读 [开发交接](./DEVELOPMENT_HANDOFF.md)，复制 [续接正文](./GOAL_PROMPT.md)。

LC-00 DONE（文档）；LC-01/02/03/06 IN_PROGRESS，LC-04/05/07 尚未开始。新队列已通过 C#174/JS60 与自有原生桥有限验证；没有取得新版真实 NX 延迟结果。LC-06 固定便携包、哈希、独立启动/停止与 Moonlight 登录页已验证，尚未配对/串流。完整本机 NX 工作流仍未通过，第二台客户端/CF 不再阻塞，TIA 和真实耐久缺项仍保留。

本轮证据及精确恢复步骤见 [输入队列候选](./LOCAL_INPUT_QUEUE_EVIDENCE.md)、[开源对照](./OPEN_SOURCE_COMPARISON.md)。为构建已停止 8091；当前 NX 观察为 070-test.prt（只读），未自动切换或保存。对照服务验证结束后也已停止，可用新 start/stop-open-source-comparison.ps1 重开/撤回。下一次真实操作须先核对可写隔离副本，并由实体键鼠观察 NX 响应，不能拿自动夹具或橙色十字作为真实输入证据。

源码同步检查点已按用户请求纳入本次提交，包含此前未提交/未跟踪的实现与文档；是否已取得推送结果应核对 origin/main 与提交记录。此前各条“未提交”描述保留为历史状态。artifacts、工程与工具缓存不随 Git 迁移。服务/窗口/句柄是运行态，换电脑不能继承，应重新核对。

补充：上一轮检查点 e93ad38af2a4a72a3a149553993b49b9825a6b64 已推送并核对远端；用户下班前要求再次同步，本轮队列、对照脚本及更新文档纳入新检查点 Improve local input scheduling and prepare isolated streaming comparison（父提交 e93ad38）。回家以 git log 与 origin/main 核对该标题或后续提交。原始报告、第三方运行目录、工具缓存和模型仍不入 Git；恢复步骤已更新 DEVELOPMENT_HANDOFF。

### 2026-09-04 LC-01/02/03 公共实现与 LC-06 独立部署验证

- 改动：网页最多一个普通操作等待应答，合并相同会话/场景/手势内连续待发 Move；保留按钮/键/滚轮顺序，紧急释放清空旧动作。原生物理桥使用容量128的 SPSC 队列，去除消费者短暂持锁导致的 LOCAL_DEVICE_QUEUE_BUSY；新增单调时钟分段统计，保留所有原生即时安全校验。
- 证据：artifacts/verification/local-input-queue-20260904-155829-9253685（C#174、JS60、MediaProbe 构建零警告，源码哈希）；local-queue-native-20260904-154832-3878619（注入排除/原生夹具接收/看门狗释放通过，物理事件0）。未声称消除了所有断线原因或达到 P04。
- 对照：锁定 Sunshine v2026.516.143833 与 moonlight-web-stream v2.10.0，ZIP哈希通过；修复实际完整配置/相对路径问题，中文路径证书失败后移至用户目录英文路径成功启动。127.0.0.1 TCP端口与浏览器登录页核验，关闭后监听0；无驱动/系统服务安装、无配对、无NX输入、无日常桌面串流。具体 commit/哈希/日志/脚本及边界见对照文档。
- 风险：网页应答串行仍受每次原生校验耗时影响；新队列实体高轮询率/复杂手势、媒体积压、同机单物理屏、Sunshine 同机焦点/输入反馈均未通过。LC-01 全链路计时、LC-04/05、真实NX/TIA/中文文件/8小时、正式发布包仍未完成，整体目标 active。
- 下一步：先完成可写隔离 NX 的新版实际采样与 LC-01 剩余计时；按证据优化 LC-04，保持安全门槛；独立对照先自有夹具同机实体输入，再NX同条件 A/B。缺实体参与时保留 WAITING_EXTERNAL 的相应真实子项，继续可独立实现的测量/逻辑工作，不标整项 DONE。

## 已确认的产品约定

- 单人在运行 NX / TIA Portal 的同一台电脑通过浏览器进行控制（2026-09-04 用户更新）。
- UG 绘图区与完整界面可切换，保留建模编辑和原生菜单/快捷键。
- 使用被控电脑本地文件及原生文件对话框。
- 软件单开满屏、双开网页左右等宽。
- 本版不要求其他电脑访问、LAN 或 Cloudflare；锁屏及登录前控制仍不实现，锁屏停输入、本机解锁后恢复要求保留。
- 目标为持续实际工作，不能按展示 Demo 的标准交付。
- 独立 Web 项目、桌面 Chrome/Edge、现有显示环境是当前设计默认值。

## 阶段进度

| 阶段 | 状态 | 现有证据 | 下一步 |
| --- | --- | --- | --- |
| 设计 | DONE | 主规划书 1.2、实现契约/依赖矩阵、SC01–SC08、验收、目标正文与资料 | 用户审阅，后续按当前状态实施 |
| M0 环境与基线 | IN_PROGRESS | 基线/doctor v2、.NET锁定构建、React/TS/Vite精确版本及npm ci/类型检查/构建通过 | NX样本既有局部实测，TIA实际可编辑能力仍WAITING_EXTERNAL |
| M1 技术探针 | IN_PROGRESS | SC02/03有限探针DONE；完整180应用重建显示自动回收；共享设备两路/退休/60次重连；NX同流记录 | 先 LC-01～07 本机低延迟增量，再 SC07/SC08 门槛汇总 |
| M2 骨架/鉴权/协议 | NOT_STARTED | 无 | M1 门槛通过后实施 |
| M3 应用/输入 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M4 布局/裁剪/弹窗 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M5 媒体/性能 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M6 恢复/异常 | NOT_STARTED | 无 | 按主文档任务依赖实施 |
| M7 真实验收 | NOT_STARTED | 无 | 不能由模拟测试替代 |
| M8 发布交付 | NOT_STARTED | 无 | 形成源码、发布包和验收报告 |

阶段状态为 NOT_STARTED、IN_PROGRESS、DONE、WAITING_EXTERNAL 或 FAILED。DONE 需要实际证据；文档内允许状态不等于平台目标工具的状态，更新工具时遵循工具本身规则。

### 已有文件的具体判定

| 任务 | 状态 | 证据与尚缺内容 |
| --- | --- | --- |
| M0-01 | DONE | legacy/demo-v3 三文件与根目录 SHA-256 一致；已有忽略规则；原文件未修改 |
| M0-02 | DONE | doctor v2、新原生显示枚举已运行；environment-20260903-m0-v2.json 包含 OS/GPU/两输出/DPI/实际 SDK/软件版本和安装快捷方式目标；不输出许可与完整参数 |
| M0-03 | DONE | .NET 10.0.400、Node24.20.0/npm11.19.0；五项目locked构建；src/web精确React/TS/Vite包锁，隔离npm ci/TSX类型检查/React运行/Vite构建通过。正式UI尚未开始；后续新增依赖继续按锁文件维护 |
| M0-04 | IN_PROGRESS | NX用户样本的隔离副本已实际编辑、取消、保存重开并核对参数；仅本机工作流，不是产品输入；TIA 条件 WAITING_EXTERNAL |
| M1-01 | IN_PROGRESS | NX同流菜单/文件框/已有特征参数框已验证；有界尺寸恢复、Windows重绑定、共享设备两路隔离和显式退休通过有限夹具；仍缺真实视图运动、GPU重置与资源耐久 |
| M1-02 | IN_PROGRESS | BoundedInputExecutor（256项/500ms、撤销清队列、独立tick）与真实CaptureInputBindings；87项输入L0、自身原生19项、最终场景87项；F0浏览器菜单/模态中文/文件框局部通过。真实NX/TIA完整键鼠、外部故障释放仍待验收 |
| M1-03 | IN_PROGRESS | 多节点Alpha、异步场景账本、JPEG、1080p和应用级捕获复用已实现；本机浏览器H264连续两次、取消后恢复、JPEG重连均显示真实夹具帧。仍缺真实动态10分钟、JPEG完整质量/资源对照及真实双路编码 |
| M1-04 | IN_PROGRESS | NX菜单/原生文件框/移动面编辑弹窗媒体与本机保存重开已具备；不是NX+TIA双应用，尚无NX完整产品输入与双窗口验收 |

源代码、截图与诊断报告均属局部技术证据。M1 不因截图或编码器 ActivateObject 成功而 DONE。当前仍无 Host、正式 Agent、浏览器控制台或发布包。

SC 子检查当前状态：SC01 DONE（纯逻辑关联/几何）；SC02 DONE（有限GPU场景、透明/遮挡/关闭/尺寸/资源观察，不是8小时）；SC03 DONE（M1有限sample/输出/回调版本与取消重建探针，非正式epoch/故障恢复）；SC04 DONE（有限媒体）；SC05 DONE（NX与F0最小输入链，详见[真实输入证据](./M1_NX_INPUT_EVIDENCE.md)）；SC06 DONE（有限NX/F0双路及H264/JPEG，见[双路证据](./M1_DUAL_EVIDENCE.md)）；SC07 IN_PROGRESS；SC08 NOT_STARTED。当前证据与边界见 [帧生命周期](./M1_FRAME_LIFETIME_EVIDENCE.md)、[资源隔离](./M1_RESOURCE_ISOLATION.md)、[场景夹具](./M1_FIXTURE_EVIDENCE.md)、[执行器](./M1_EXECUTOR_EVIDENCE.md) 和 [F0浏览器](./M1_BROWSER_INPUT_EVIDENCE.md)。不得因一个SC项完成提前进入正式M2。

## 已知外部依赖与风险

| 编号 | 条件 | 当前信息 | 处理方式 |
| --- | --- | --- | --- |
| E01 | NX 可采集/可编辑 | 已有隔离副本本机编辑/取消/保存重开和1080p参数框同流证据；尺寸变化故障已有限恢复实测 | 继续受限NX输入与完整工作流，N0新建/撤销等未覆盖项保留 |
| E02 | .NET 10 SDK | 项目本地 10.0.400 版本命令成功；已不缺 SDK 本体 | 验证完整锁定构建；不重复下载安装 |
| E03 | H.264 硬件编码 | Intel Quick Sync 已接真实 WGC/D3D 源并在本机浏览器短时显示；测试图历史十分钟通过 | 继续真实动态场景、1080p、双路和正式质量/恢复测试 |
| E04 | 博图安装与许可证 | WAITING_EXTERNAL：本次未验证有可用实例 | 用户准备所需 STEP7/WinCC 组件与有效授权，记录具体版本/更新；恢复 M0-04.tia，再执行 T0 离线编辑、编译、保存重开，不连接真实 PLC |
| E05 | 博图系统匹配 | WAITING_EXTERNAL：本机 Home 25H2；官方所列 Home 24H2 仅基础版 | 用户确认厂商支持的系统/组件组合；复核安装清单后恢复 TIA 分支，不擅自改 OS |
| E06 | 真实工程副本 | N1已提供070.prt，隔离副本及原始/保存后哈希见真实模型增量；T1仍WAITING_EXTERNAL | N1继续按M7核对代表性负载，不因本机短时编辑通过全部验收；用户提供离线TIA测试工程后恢复T1 |
| E07 | 第二台客户端 | SUPERSEDED_BY_USER_SCOPE：本版仅本机访问 | 不再等待第二台客户端；换电脑开发按 DEVELOPMENT_HANDOFF 恢复环境 |
| E08 | CF 入口 | SUPERSEDED_BY_USER_SCOPE：本版不要求公网 | 保留历史记录，不计通过、不阻塞本版 |
| E09 | 工具原生桌面操作能力 | Computer Use 已恢复并观察 NX；尚无完整输入工作流 | 用工具辅助真实验证；工具画面不能冒充项目采集/输入证据 |
| E10 | CF 公网持续画面流条件 | SUPERSEDED_BY_USER_SCOPE | 不再要求 CF 账号/域名/方案；不计通过 |

这些不是设计阶段的阻塞；不得因此把实施任务擅自标记完成。

## 实施循环记录模板

每个连贯任务追加一条：

```text
日期/时间：
任务编号：
状态：
本次修改文件：
验证命令或真实操作：
实际结果：
证据路径：
剩余风险/外部依赖：
下一项：
设计变更（若有）：
```

## 本轮设计复核记录

- 任务：设计 1.1。
- 修改：主规划书、实现契约（新增）、验收标准、目标正文、资料依据、实施状态。
- 检查：PPT 第 5–7 页全文；Demo 静态源码与副本哈希；项目工具版本；Windows/CF/WebCodecs/Siemens/OpenAI 官方资料。
- 设计修订：Host/Agent 故障双向释放、静止画面与断流区分、跨通道代次、输入/场景事务、禁止异机不安全 HTTP 远控、CF 公网服务条件、36 项任务依赖。
- 证据边界：仅文档和只读检查通过，不增加真实功能 PASS 项。

## 2026-09-03 15:16 实施记录

- 任务：M0-02、M1-01、M1-03（后两者仅局部进展）。
- 源码：修复 D3D11 公共重载；新增 EncoderCatalog、DisplayCatalog；补窗口身份/可见子节点/客户区；探针提供安全报告输出、取消、尺寸变化失败提示；新增 verify-probe.ps1，补全 doctor v2。
- 验证：`.\scripts\verify-probe.ps1 -CaptureNx` 两轮通过；最新 `artifacts/verification/probe-20260903-151616-2305538/report.json`，9 项 PASS；构建 0 错误/0 警告。该套件是冒烟测试，不是 34 项验收矩阵。
- 原生证据：`m1-wgc-nx-first.png/.json`；`m1-encoder-activation.json`；`environment-20260903-m0-v2.json`；详见 M1_INITIAL_EVIDENCE.md。
- 保护：原 Demo 三个 SHA-256 仍一致；NX 原模型只观察、未保存/修改；没有改变显示/许可/CF 配置。
- 风险：显示输出 DPI 与旧 NX 窗口 DPI 不同，不能假定单比例；第二次采集期间前台/模型位置变化，未当成受控遮挡通过；硬件激活仍不等于编码能力。
- 下一步：M1-03 实际 H264 输入/输出和浏览器最小解码链，准备 Windows 测试夹具；在可控桌面时段补 NX 菜单/弹窗、动态和输入验证。

当前没有后台测试进程待轮询。续接时读最新报告并检查源码，从上述下一步继续；不重做 M0-01，不重新安装工具链，不把 TIA/第二客户端/CF 待验证项写成通过。

## 2026-09-03 设计规划书交付校订

- 本轮响应“先出具完整规划书”的要求，只复核文档与现有证据，未修改业务源码、操作 NX/TIA 或新增实施目标；已有完整目标状态未改动。
- 主规划书与 REFERENCES 更新了双显示器/DPI、已构建探针、真实首帧及编码器激活的证据边界，修正落后于 15:16 报告的文字。
- 规划仍为 1.1；36 项任务、34 项功能/安全验收、13 项性能/稳定性目标与协议契约不变。设计交付不改变 M0/M1 的 IN_PROGRESS 状态，不新增功能 PASS。
- 后续编码入口：GOAL_PROMPT 的推荐目标正文或已有目标续接正文，必须连同 IMPLEMENTATION_CONTRACTS 读取；先完成当前 M1 缺项，再按依赖推进正式功能。

## 2026-09-03 M1-03 实际编码与浏览器链路

- 上一目标轮属于 progress（校正权威设计/证据状态）；本轮新增实际源码与真实编码/解码证据，仍为 progress。目标继续 active，未缩小范围。
- 新增 H264Probe、AnnexBAccessUnits、Workbench.MediaProbe、20 项单元测试及媒体/HTTP/资源脚本；扩展 CLI，保持原 Demo 不变。
- 真实 Intel 1280×720 编码经本机 WS/WebCodecs 连续十分钟，18000 帧全量解码，601 次动态像素检查零错误；源码边界保护更新后的版本另做短时复测。
- 验证路径、构建哈希、原生错误修复、采样范围与局限见 [M1_MEDIA_EVIDENCE.md](./M1_MEDIA_EVIDENCE.md)。P01–P13 没有因此被整体标记通过；尤其不是输入时延、真实工程工作流或八小时证据。
- `verify-media.ps1`：20 项逻辑测试、硬件/软件各 90 帧、无效参数不产物与禁止覆盖通过。`verify-probe.ps1`：四项目 locked restore/build，0 警告/0 错误，8 项冒烟通过（未碰 NX）。
- 取消后 busy=false，同一个服务进程可重新申请编码。停止按钮不是产品租约/故障安全释放的实现，因为此工具不注入输入。
- 下一步：将实际 WGC/D3D 纹理接入该编码路径，补 GPU 转换与 Windows 输入夹具，再回到真实 NX 菜单/弹窗/建模和 M1-04；保持 TIA/第二客户端/CF 等待外部环境的状态。

15:54 收尾：最终版本取消后同进程一分钟重连通过（1800 帧、61 次像素检查、0 错误），报告 `media-probe/run-20260903-155200-3274576.json`。HTTP 四项复测通过。测试服务和资源采样均已结束，临时浏览器页已关闭，没有后台测试等待续接；无需重新运行已经通过的测试图十分钟试验来代替真实 WGC 源接入。

## 2026-09-03 完整规划书交付复核

- 响应本次“先出具完整任务规划书、后续采用目标方式实现”的请求，当前交付为文档复核，不继续原生 UI 操作或业务编码，不新建/完成目标。
- 已再次读取 PPT 实际第 5–7 页完整文字、原 Demo、主规划书、实现契约、验收与目标正文；Demo 三文件与 legacy 副本 SHA-256 仍一致。
- 修正主书第 2.1 节落后于媒体报告的表述，新增第 14.1 节实施续接检查点；36 项任务和 A01–A34、P01–P13 不变。
- 工作树已有尚未形成真实运行证据的 `WgcNv12Source.cs`、`IProbeFrameSource.cs`、H264 `sourceFactory` 接口及 CLI 临时 `--inspect-graphics-api` 分支。本次只确认源码存在，不把此前测试图构建的通过结果归到这些增量，也未在本次复核中测试它们。
- 下一实施步骤：完成真实源入口、颜色与资源生命周期检查，锁定构建并回归原路径，取得真实 NX → WGC/D3D → H264 → 浏览器证据，再补输入/弹窗与 M1 门槛。原生环境届时重新枚举，不沿用历史 HWND 或最小化状态。
- 外部条件仍为 TIA 安装/组件/授权及系统匹配、典型工程副本、第二客户端和 CF 环境/使用条件。它们不阻止当前设计交付；不得因此把完整实施目标标为完成。

## 2026-09-03 M1 真实 WGC/GPU 源实施

- 上一轮分类为 progress（规划与真实源码续接点已校正）；本轮实现真实 GPU 源入口、低延迟与色彩配置，并形成实际浏览器证据，仍为 progress。完整目标继续 active。
- 主要文件：WgcNv12Source、IProbeFrameSource、GraphicsCaptureInterop、H264Probe、两个 Probe 入口、媒体页面/说明、回归脚本和单元测试。临时 API 反射分支已移除。
- 真实结果：NX 源 10 秒及 60 秒本机浏览器测试，分别 1 帧及 7 帧全量解码；静止源按变化出帧，不写成 30 FPS。MF 低延迟属性使静止首个编码输出无需等待排空。
- 发现并保留缺口：NX 文件菜单为独立 owned HWND，主流不含菜单；单独 WGC 截图和 GPU 编码成功。A15 尚未通过，不把主窗口媒体 PASS 视为完整支持。
- 回归：23 项测试、硬件/软件各 90 帧、8 项构建冒烟、8 项 HTTP/真实 WS 握手拒绝检查通过。最终构建真实源取消后同进程重连成功。完整路径与二进制身份见 [M1_WGC_GPU_EVIDENCE.md](./M1_WGC_GPU_EVIDENCE.md)。
- 保护：仅恢复 NX、展开/收起文件菜单作观察，没有建模、保存或关闭工程，没有新增公网入口。工具对 NX 的原生操作不能当作项目输入链证据。
- 下一项：关联窗口场景捕获/合成与 Windows 夹具，进而完整输入、NX 文件框/编辑参数框、双窗口与 JPEG。M2 正式服务仍等 NX 技术门槛，不重跑测试图十分钟替代这些缺项。

16:28 收尾：最终构建真实源取消后重连及测试图 300 帧 / 11 次像素检查通过，完整文件名与哈希见本轮证据文档。测试服务及相关进程均已结束，临时页面已关闭；没有后台实验需要等待。NX 恢复为可见窗口，菜单收起，未修改/保存模型。下轮直接检查现有源码并推进关联窗口场景，不重复设计交付。

## 2026-09-03 规划书 1.2 交付

- 响应当前“先出具完整任务规划书、后续再实施”请求，只修改六份设计/状态文档；没有继续业务编码或原生 UI 操作。已有完整实施目标仍 active，没有新建、完成或修改目标范围。
- 核对：源 PPT 第 5–7 页完整文本；Demo 与 legacy 三文件哈希；真实源证据记录、菜单窗口快照、当前固定场景版本代码；Microsoft/Siemens/CF/W3C/OpenAI 官方资料。
- 修改：主书第 2.1/15.1 节更新真实源证据；新增 6.3/15.2；契约补充 4.4 与 SC01–SC08；同步验收子场景、版本和后续目标正文。详见主书设计变更记录。
- 设计影响：关联窗口准入、真实边界、透明混合、节点资源及异步编解码的场景标签不能留给后续代理猜测。36 个主任务、34 个 A 项和 13 个 P 项不减少。
- 验证：主书 36 项任务与契约 36 行依赖一一对应，无重复或差异；A 项 34、P 项 13、SC 子项 8；21 个文档本地链接无缺失；Demo 原件与副本的六次哈希检查全部一致。这是文档一致性检查，不增加功能 PASS；M1 仍 IN_PROGRESS，M2 以后不提前放行。
- 下一实施入口：现有源码与最新状态核对后按 SC01 起推进，后续完成项以实际记录为准；无须重复生成整份规划、解压或下载工具链。

## 2026-09-03 M1 关联场景实施

- 上一轮完成设计 1.2 复核；本轮依据现有完整实施目标推进 SC01–SC04，分类为 progress。没有重新创建目标，完整目标仍 active；未改变 36 项任务或 A/P 验收门槛。
- 新增 OwnedWindowScene、WgcOwnedNv12Source、GpuSceneCompositor、FrameSceneLedger 和浏览器 scene-timeline；扩展窗口元数据、CLI/媒体入口、回归脚本及测试。
- 真实失败驱动修正：不透明实验在 NX 菜单 Layered 阶段失败，随后实现预乘 Alpha GPU 混合并通过真实显卡像素校验；未静默丢菜单或退到整桌面采集。
- 同流真实证据：一分钟 NX 文件菜单 8 帧，节点 1→2→2→1；打开文件框静止 1 帧/2 节点；取消文件框一分钟 17 帧，节点 2→1、主窗恢复 Enabled。浏览器均实际解码和视觉核对。工具操作不是项目键鼠输入，未加载新文件或编辑/保存原模型。
- 最终回归：44 项 C#、5 项 JS、硬件/软件各 90 帧、8 项构建冒烟、8 项 HTTP/WS 边界全部通过。四项目 locked restore/build 0 警告/0 错误。所有报告路径、二进制身份、失败观察和局限见 [M1_SCENE_EVIDENCE.md](./M1_SCENE_EVIDENCE.md)。
- 取消后 busy=false，同一服务实例重连真实源 10 秒成功；最终构建默认测试图 300 帧全量解码、11 次像素检查 0 错误。没有把这些结果写成输入释放、丢包恢复或真实工程持续操作通过。
- 下一项：Windows 场景夹具、受控透明/遮挡/资源对照和 NX 编辑参数框，然后完整产品输入、双窗/JPEG、真实十分钟动态与 1080p，形成 M1 放行报告。TIA/典型工程/第二客户端/CF 条件继续按外部依赖表处理。
- 原 Demo 与 legacy 三份 SHA-256 全部仍一致；PPT 未改。所有临时媒体服务均已停止，临时浏览器页已关闭。NX 文件菜单/文件框已收起，原只读工程保留；续接无需等待历史后台测试。

## 2026-09-03 Git 交接检查点

- 按用户要求暂停功能开发，准备提交到 `https://github.com/XiaoyeCodes/TwinDesk`，明天继续。远端现有 `main` 初始化提交及 LICENSE 保留，不强制推送、不改写历史。
- 本次续接仅检查了状态、实现契约、项目配置和 Git 状态；用户打断前没有新增 Windows 场景夹具源码，也没有启动测试进程。不要把已宣布的下一步当作已经实现。
- 提交范围为源码、锁文件、脚本、设计/验收/状态文档和原 Demo。`.tools`、`artifacts`、构建目录、安装包、截图和本机敏感配置不进 Git。已有实测报告留在当前电脑，仓库里的报告链接不代表新电脑已具备这些本地文件。
- 明天优先读取本文件及 M1_SCENE_EVIDENCE，从 SC02 Windows 场景夹具/受控透明和遮挡验证继续，再补 NX 编辑参数框与产品输入。SC01 不重做；M1 未放行，完整项目未完成。
- 换电脑克隆后的工具准备与构建说明见 [REPOSITORY.md](./REPOSITORY.md)。不得为复现而自动安装/修改 Siemens 许可证或操作真实 PLC。

## 2026-09-04 Git 暂停与续接检查点

- 按用户“先提交推送，明天继续”的要求停止实施，把现有 Windows 场景夹具和 F0 交互测试目标作为阶段性增量保存。不代表 M1 放行或完整项目完成。
- 本次提交新增 `Workbench.DesktopFixture`、`verify-scene.ps1`，并为场景源增加仅供夹具调用的诊断像素读回及捕获节点计数。正常媒体路径不调用该读回方法；F0 只接收自身窗口事件，尚未实现产品远程输入。
- 提交前验证：五项目构建成功，0 警告、0 错误；44 项 C# 单元测试全部通过。当前没有重新进行真实 NX/TIA 或完整媒体端到端验收。
- 本机已有场景报告 `artifacts/verification/scene-fixture-20260904-085917-6301863/report.json`：40 轮窗口开关、87 项检查 PASS。该报告产生于添加交互 F0 窗口之前，不应归为当前最终夹具二进制的重新验证；资源数据仅为短时观察，不代表长时间无泄漏。
- F0 媒体观察报告位于 `artifacts/verification/media-probe/run-20260904-091020-4104978.json`，续接时应完整核对再作结论；本次交接不据此新增输入、模态操作或 NX 工作流 PASS。相关临时媒体服务和夹具进程已停止。
- 明天先读本检查点、实现契约和夹具 README，复测最终夹具版本，核对 F0 媒体结果，再推进真实 NX 编辑参数框与产品输入链。操作原生软件前重新枚举窗口，不复用历史句柄；M1 未通过前不提前放行后续正式服务。
- `.tools`、`artifacts`、构建输出、本机配置继续忽略，不上传安装包或运行截图。保留远端已有历史，使用普通推送。

## 2026-09-04 M1-02 输入核心与夹具复测

- 接到明确目标续接后，在干净的 `7022827` 上建立本地 `codex/m1-nx-input` 分支；本轮未提交或推送。上一 Git 交接轮和本轮均属 progress，完整目标保持 active。
- 场景夹具首次复测因“测试遮挡窗未实际覆盖参考点”失败，保留报告；只把自身临时遮挡窗置顶并增加实际命中诊断后，最终构建 40 轮共 87 检查通过。活动捕获归零，但原生句柄/内存未完全回初始值，长时资源验证仍未完成。
- 新增 InputCommand、PhysicalKeyMap、SceneInputCoordinates、InputSession、两份测试和 verify-input 脚本。实现仅 L0 输入安全核心，原生后端/输入 WS/浏览器事件尚未连接；SC05 仍 NOT_STARTED，不提前进入正式 M2。
- 最终证据：`input-core-20260904-092509-0089998` 定向 49 项（47 新增+2 既有）；`media-20260904-092624-3928431` 全量 91 C#、5 JS、硬件/软件编码各 90 帧；`probe-20260904-092512-5193409` 五项目构建与 8 冒烟；`scene-fixture-20260904-092537-1869175` 场景 87 检查。均在本机 artifacts/verification，来源哈希见报告。
- Computer Use 观察 NX 空白工作区及新建框；控件索引失效和焦点不可靠后取消，没有创建/修改/保存模型。N0 临时目录仍空；不能把新建框当作特征编辑框。上一 F0 一分钟报告只含菜单 3→1 节点、3 帧，未记录模态编辑框，不新增该项 PASS。
- 详细实现、失败和边界见 [输入核心](./M1_INPUT_CORE_EVIDENCE.md) 与 [场景夹具](./M1_FIXTURE_EVIDENCE.md)。原 Demo 与 legacy 六次哈希一致，PPT 未改；无本轮遗留测试进程。
- 下一步直接实现 M1-02 受控原生后端、输入环境检查、实际部分提交/Unicode 抬键与有界执行器，再接 F0 浏览器输入。SC04 真实参数框、双窗/JPEG、真实十分钟与 1080p、Host/Agent 故障保护、真实 TIA/第二客户端/耐久仍缺，不缩小完成条件。

## 2026-09-04 原生输入增量与 Git 交接

- 按用户最新要求停止功能开发，将当前 `codex/m1-nx-input` 增量提交并普通推送到远端 `main`，保留原有历史。当前段为续接检查点，不是完整目标完成声明。
- 新增 NativeInputBackend、NativeInputEvents、WindowsInputEnvironment、原生自测及回归测试；实际 SendInput 只在明确启动的自身测试窗口中验证，没有操作 NX/TIA。证据边界见 [M1_NATIVE_INPUT_EVIDENCE.md](./M1_NATIVE_INPUT_EVIDENCE.md)。
- 已落盘的最终报告：`native-input-20260904-093953-0351261` 为 19 项 PASS；`media-20260904-094147-9458926` 为 108 C#、5 JS、硬件/软件各 90 帧及边界检查通过；`probe-20260904-094050-6755895` 为构建与 8 项冒烟通过。均位于本机 `artifacts/verification`，本次交接只核对已有报告，不重跑原生软件测试。
- 早期心跳释放实测 6062 毫秒超过 6 秒目标，已保留失败边界说明；内部阈值改为 5.5 秒，最终自身窗口实测 5546.244 毫秒。单次受控结果不代表完整 P07 或真实网络/进程故障矩阵通过。
- 明天从有界输入执行器、真实捕获窗口生命周期绑定及 F0 回环浏览器输入联调继续，再推进 NX 参数框；不能用恒真身份校验回调绕过绑定验证。SC05–SC08、正式 M2 状态不提前更改。
- 原 Demo、PPT、许可证和公网配置不修改；构建输出、测试产物、安装包和本机凭据不上传。历史记录中的“尚无原生后端”仅描述当时状态，以本检查点为准。

## 2026-09-04 M1-02 有界执行器与真实捕获绑定

- 从 main/bdb7850 干净工作树直接续接，未创建重复目标、未提交/推送；目标保持 active。
- 新增 BoundedInputExecutor、CaptureInputBindings 与 19 项 L0 检查，真实 WGC 节点关闭/首帧/合成接入绑定桥；场景夹具每次合成复核绑定。详见 [执行器证据](./M1_EXECUTOR_EVIDENCE.md)。
- 实测 `input-core-20260904-095048-3842451`：85 项 L0 通过；`scene-fixture-20260904-095052-8873701`：40 轮 87 项自身 WGC/GPU 检查通过。构建 0 警告/错误；不是网页输入或真实 NX/TIA 验收。
- 剩余风险：原生调用卡死时单线程无法担保 6 秒释放，必须后续 Host/Agent 外部看护；捕获身份检查不能消除全局 SendInput 的瞬时焦点竞争；正式 UI/协议/鉴权未实现。
- 下一步：执行器 + 捕获绑定的自身原生输入集成，再 F0 回环浏览器链与 SC04 NX 参数框。不降低 SC/M1/正式验收门槛。

09:54 原生增量：自身输入自测已改用 BoundedInputExecutor 和持续真实 WGC 的 InputBindings.Verify，不再使用测试窗口恒定代次回调。`native-input-20260904-095230-7484699` 的 19 项检查通过，心跳释放 5495.2784ms；显示 ACK 仍为本机自测模拟，不能当网页输入证据。127 项 C#/5 项 JS 与硬软编码各90帧回归、五项目构建/8冒烟通过，详见执行器证据。下一项为回环浏览器控制/视频分通道、场景冻结与真实显示 ACK。

## 2026-09-04 M1-02 F0 浏览器输入链

- 新增仅本机 F0 的独立控制 WS、浏览器输入适配、真实显示 ACK/冻结事务与重连生命周期；普通媒体启动保持只读，未放开 NX/TIA 或 LAN。增加 `start-input-probe.ps1` 与8项JS L0。
- 实际右键触发多节点几何拒绝，复现后修复 ZOrder 归一化方向错误并添加回归；失败与报告缺口明确保留，不修改验收标准。
- 两次同服务浏览器实验分别为 `media-probe/run-20260904-100110-8381974.json`（44帧/15提交，右键子菜单、模态中文、取消）和 `run-20260904-100241-4017976.json`（91帧/12提交，原生文件名中文与取消）。真实解码画面可见反馈；不是截图替代工作流，也不是 NX/TIA 或 P04。
- 详细观察、失败及范围见 [F0 浏览器证据](./M1_BROWSER_INPUT_EVIDENCE.md)。报告后补了迟到旧 ACK 前端冻结回归，当前最终构建仍需短时浏览器复测；130 C#/13 JS 与硬软编码90帧回归、五项目构建/8冒烟通过。
- SC05 仅新增其 F0 子检查的局部技术证据；SC04 NX 仍缺参数框，不提前把 SC05–SC08 或 M1 标记通过。下一项：最终浏览器/HTTP边界复测，然后 SC04/N0 真实 NX 参数框与编辑能力。TIA、客户端、公网/条件与8小时仍待外部或后续工作。
- 工具操作只启动/观察测试夹具；本轮未修改 NX/TIA 工程。某次直接 dotnet 构建未加载 environment.ps1，SDK 首次运行输出提示创建了开发HTTPS证书；未信任该证书、未改变浏览器安全检查。其后恢复使用受控环境脚本，不能把此证书当作已部署LAN TLS。

10:09 最终复测：`scene-fixture-20260904-100507-8067183` 87项；启动脚本重新枚举F0成功，输入WS7项与媒体HTTP/WS8项通过。`run-20260904-100852-7357235.json` 最终构建一分钟248帧/3场景，网页模态中文与取消可见，19次提交无拒绝，停止后held=0/queued=0。均非真实NX或性能PASS。脚本最初类型转换失败已修复且保留失败目录，详见F0证据。下一步M0-04.nx/SC04临时N0参数框，不重复这些回归替代缺项。

## 2026-09-04 SC04 本机验证准备

- 新增 `start-nx-scene-probe.ps1` 与 [NX核对步骤](./M1_NX_VALIDATION.md)。实际 `-PrepareOnly` 完成锁定构建/重新枚举/独立样本目录，`sc04-nx-20260904-101130-2261347/preparation.json` 明确标为 PREPARED_NOT_UI_VERIFIED / NOT_RUN；目录中未创建模型。不因准备成功增加真实验收PASS。
- Computer Use 本轮只读观察确认 NX 仍为空白工作区。随后该工具连续报告 `user input was detected in this window; call get_window_state before continuing`，刷新后仍拒绝 F0 关闭操作；停止桌面输入，不转用其他注入工具绕过本地活动保护。NX 未创建/修改/保存文件。
- 最终测试服务已停止，停止前控制账本held=0、队列0；自身临时F0通过启动它的命令会话终止，不涉及NX/TIA或用户工程。原生UI续接前重新观察并确认安全时段，不复用本段句柄。临时浏览器页不再连接输入服务。
- 下一项优先SC04/N0；若本地桌面仍忙，继续不依赖它的M1 JPEG/媒体诊断等通用任务。TIA、真实样本、第二客户端与CF仍按外部依赖表恢复。完整目标active，不能把当前探针当成第一版发布包。

## 2026-09-04 M1-03 JPEG 兼容实施

- 前一轮有界输入与F0实测属progress；本轮继续现有脏工作树，没有覆盖用户文件或提交/推送。新增JpegFrameEncoder、JpegProbe、浏览器jpeg-decoder、6项C#/5项JS；共享WGC合成拆分，JPEG单独GPU缩放/CPU读回，默认H264仍GPU NV12。说明和真实证据见 [JPEG增量](./M1_JPEG_EVIDENCE.md)。
- NX工具恢复了打开/取消新建框，但编辑字段索引失效，文本选择没有可靠反馈，取消后空白工作区保留；没有创建或保存N0。这个具体工具缺口尚需可靠本机操作或用户按步骤补测，不视为NX功能通过。
- 最新完整回归 `media-20260904-101701-2754680` 为136 C#/18 JS、硬软H264各90帧；`probe-20260904-101711-4175082` 五项目0警告/错误及8冒烟；`scene-fixture-20260904-101917-5310493` 40轮87项，均通过各自范围。
- 真实NX JPEG `run-20260904-101856-7245486.json` 一分钟10帧、4场景，文件菜单在同流出现/消失；H264 `run-20260904-101959-9725344.json` 10秒1静止帧。F0 JPEG `run-20260904-102201-4054080.json` 一分钟32帧/3场景/7输入，中文模态和取消可见，结束held=0。8项媒体及7项控制边界通过。所有帧率配置、首帧时间与ACK不充当正式性能/工作流通过。
- 无实质路线变更，JPEG正是既定兼容分支。SC04仍缺真实参数框，SC05/06主项依赖不提前放行；未进入正式M2。下一项优先可靠N0/SC04和受限NX输入，其次M1双路/1080p与真实动态；不重复单纯测试图填补真环境缺口。

10:23 收尾：本轮媒体服务和自身F0会话均已终止，8091无监听；NX新建框/文件菜单已取消或收起，未创建或保存模型。原Demo三份与legacy副本哈希仍一致，`git diff --check` 无错误。最新3份浏览器报告已读回核对，未修改其原始内容。下轮直接依据上述缺项继续，不重新下载SDK或重复需求访谈；完整目标保持active。

## 2026-09-04 10:37 用户样本 / SC04 参数框 / 1080p

- 用户提供070.prt后不再等待N1路径；保留原件，在独立目录建立只读备份和可写副本。实际创建移动面距离1，重编辑距离2预览并取消，保存关闭重开后参数仍为1；原件/备份哈希未变，当前测试副本已保存、编辑框已取消。
- 新增固定720p/1080p档位及尺寸验证，143项C#/20项JS、硬软90帧回归、五项目构建/8冒烟通过。真实NX 1080p两段一分钟分别101帧/19场景和106帧/5场景，实际参数与取消消失可见。是本机原生操作+只读媒体，不是网页输入/性能/8小时PASS。
- 保留一段80帧后采集尺寸变化失败；唯一主窗脚本已排除真实观察到的SysShadow，未放宽多实例选择。详细过程、原始报告和构建哈希见 [真实模型增量](./M1_NX_MODEL_EVIDENCE.md)。
- 下一项：采集尺寸变化有界重绑定、720p/1080p JPEG回归，再受限NX输入；SC04局部缺口已补但可靠恢复仍缺，不提前放行完整M1。完整目标active，本轮progress，无提交/推送。

10:44 后续：尺寸变化有界重绑定及4项预算单测完成。Windows40轮91项实测通过，其中4次尺寸恢复及旧输入绑定拒绝；包装脚本旧计数错误的失败已保留并修复。147 C#/20 JS、硬软90帧与五项目构建/8冒烟通过。最终NX1080p硬编一分钟68帧/8场景/3次实际恢复，参数框折叠/展开/取消持续可见；JPEG一分钟49帧/10场景，菜单/文件框/参数显示可见，但参数取消晚于结束，不虚报同流取消。最终副本哈希、各构建身份和原始路径见真实模型增量。下一项按SC02/03剩余缺项及SC05依赖继续，样本不再等待；目标保持active。

10:46 收尾：默认720p H264/JPEG各10秒1静止帧真实浏览器回归通过，路径见真实模型增量；不是动态帧率PASS。媒体/夹具已停止，8091无监听；NX停留在已保存的070-editable.prt，所有本轮参数框/文件框已取消。原件与只读副本哈希未变；最终可写副本哈希F4EE46EF…CC1E1F，完整身份见证据页。原Demo保持一致、diff检查无错误，无提交/推送。续接优先SC02资源边界/SC03延迟输出缺项核对，再按依赖推进SC05真实NX输入；不重复已完成的模型准备或短时媒体实验。

## 2026-09-04 10:58 SC03 帧生命周期增量

- 从当前状态推进SC03，未重复准备用户模型、未操作NX/TIA或原始070.prt。新增同场景序号退休、关闭/重建隔离、JPEG所有权、单帧延迟故障注入及有限Windows场景生成器。目标保持active，本轮progress，无提交/推送。
- 首轮报告超时、第二轮94解码仅92呈现均保留为失败；到期检查、Window计时器调用包装与输出异常保护修正后，最终H264 89解码/88呈现/1旧场景退休，JPEG 52/51/1，分别15场景。JPEG取消后同服务新流51帧/15场景通过。不能将这些结果称为NX工作流、网络性能或8小时PASS。
- `media-20260904-105511-7898122` 为147C#/32JS、硬软90帧回归；`probe-20260904-105524-2257930` 五项目0警告/错误、8冒烟。构建哈希、逐份失败/成功报告和可运行步骤见 [SC03增量](./M1_FRAME_LIFETIME_EVIDENCE.md)。
- SC02/03仍IN_PROGRESS。SC02已有91检查，但源Dispose后的进程句柄/私有内存未回初始，需隔离诊断读回与GC影响；SC03仍需主路取消/重连与编码侧延迟/身份恢复审计。下一项优先这两个明确缺口，再接NX副本浏览器输入，不重复短时媒体充当真实任务。正式M2、TIA/第二客户端/CF及耐久状态不变。

10:59 收尾：最终8项HTTP/WS边界通过（`media-probe/http-20260904-105901-3547512.json`）。媒体服务已停止、自身有限夹具已退出，8091无监听；本轮只读浏览器页可保留报告但不连接输入。070原件/只读备份仍为B5191EEA…81B19D3、已保存可写副本仍为F4EE46EF…CC1E1F；原Demo三份哈希一致，diff检查无错误。下一轮从SC02隔离资源及SC03主路恢复缺项继续，不重复样本准备。

## 2026-09-04 11:12 SC02隔离 / SC03主路完成

- 前轮代码/真实回调实测属progress；本轮继续现有工作树，新增自身资源隔离程序、句柄类型计数、事件token A/B和编码后有界延迟。没有操作NX/TIA/用户原件，未提交推送；完整目标active。
- 三轮40次完整采集排除CPU读回后，Event在诊断GC后168→209→251；纯窗口371句柄稳定；仅item的Event120→120→120；加Closed订阅/取消变为161→201→241。原生token相同现象162→202→242。精确报告、hash及接口来源见资源隔离页；这些是失败定位证据，**不是资源PASS**。
- “显式释放item原生引用”试验无效，已撤回；未删除Closed检查或采用强制GC生产补丁。原生token helper仅供诊断，没有接入实际采集；不能据A/B结果断言Windows或CsWinRT唯一根因。
- SC03新增实际编码后延迟：47帧全解码，45呈现/2旧场景退休；主路取消后同服务重建47帧，46呈现/1退休。L0账本/取消隔离、两格式浏览器延迟及主路重建已满足SC03有限探针范围，标DONE；正式epoch/IDR、故障和输入释放仍是后续必需任务，M1不放行。
- 最终 `media-20260904-111128-4662707` 为152C#/32JS、硬软90帧回归；`probe-20260904-111142-3703396` 五项目0警告/错误、8冒烟。媒体/自身夹具已停止，8091无监听；无需等待历史会话。
- 下一步从Closed最小复现继续，比较关闭前/实际关闭回调后撤销及独立原生委托marshal，找到保持关闭安全通知的有效修复；再复测SC02/真实NX并接产品输入。TIA、第二客户端、CF与耐久外部/后续条件不变。

## 2026-09-04 11:29 SC02 关闭通知与资源修复

- 独立COM sink绕过CsWinRT后仍每40窗口增加40 Event，而sink实例归零；仅item等待Closed3秒失败也保留。没有继续采用无效引用释放补丁。
- 设计变更：实际采集改为每源一个、按目标PID过滤的原生窗口销毁hook及独立消息线程，配合每次采样HWND/PID检查、100ms精确场景身份重枚举。不是取消关闭安全要求，不改WGC/H264路线，不依赖强制GC；详情和微软接口依据见资源隔离页。
- 最新 `capture-resources-20260904-112740-4266343` 三轮40次各81场景，120个真实通知在不调用采集时撤销旧绑定；另1次根窗关闭后拒绝旧流，Dispose活动捕获0。Event129/130/131，总503/506/509，原每窗口递增消失但仍不能宣称8小时有界。根关闭2.176ms只是本机夹具观测，不是P04。
- `scene-fixture-20260904-112615-1959983` 91项通过；新增保守Alive/回调边界之后 `media-20260904-112701-7466394` 152C#/32JS、硬软90帧回归通过。新增根关闭夹具的一条nullable警告已修复；最终 `probe-20260904-112827-2606527` 五项目0警告/错误、8冒烟。各原始报告保留各自DLL身份，不改历史。
- SC02/SC04暂保持IN_PROGRESS，下一项最终NX媒体和较长资源观察后汇总，再SC05受限NX输入。未操作NX/TIA工程或原件、未提交推送；目标active。所有独立工作仍未完成，不能按外部阻塞结束整项目标。

11:39 后续：`capture-resources-20260904-112955-0232178`扩展三轮各120循环/241场景和根关闭；每源无循环内GC，360次销毁撤销通过，但Event129/143/147、总503/553/556出现多类句柄阶跃，保留未归因。同期桌面观察和NX服务可能构成环境干扰，只记录共现不宣称根因。真实NX `run-113231` 菜单/参数取消同流60帧通过；`run-113326`仅文件框显示、取消过晚；`run-113447`有本地用户窗口变化不作受控验收；`failed-113522`参数错误待定位。完整路径、哈希及界限见资源/真实模型页。

Computer Use检测用户输入后已停止桌面动作，未绕过；媒体服务和自身资源夹具已结束，8091无监听，原始及两份副本哈希保持此前记录。本轮最后补失败报告调用栈/HRESULT/帧数/场景上下文，`probe-20260904-113818-8427302`五项目0警告/错误及8冒烟通过；这不等于异常已修复。下一项优先自身瞬态窗口/资源阶跃隔离，安全时段用新诊断复现NX异常。不要再从COM引用释放猜测重做，也不要重复准备070样本。目标继续active、无提交/推送、未修改必需验收条件。

## 2026-09-04 11:52 SC02瞬态窗口 / SC04复测

- 本轮从11:39检查点继续现有工作树，目标仍active；未提交、推送或重复准备样本/SDK。
- 确定性自身原生夹具复现枚举后关闭导致CreateForWindow E_INVALIDARG，新增仅关联弹窗已消失时的有限重试，保持根窗口消失拒绝旧流。20次恢复+1次根关闭检查通过，详见[竞态证据](./M1_TRANSIENT_WINDOW_EVIDENCE.md)。历史NX错误缺栈，不能据此断言同源。
- 152项C#/32项JS、硬软90帧及91项场景回归通过。真实NX同服务两次1080p一分钟分别65/54帧全部解码呈现，文件框/取消和参数框/取消分项可见；不是产品输入、动态帧率或耐久PASS，见真实模型页。
- 无并行媒体/桌面观察时的360循环报告 `capture-resources-20260904-114352-3369867`：Event129/131/135，总句柄514/519/525，私有内存57,499,648/61,108,224/58,941,440字节（各轮诊断GC后）。仍有线程/ALPC/Event小幅变化，未证实完全有界。
- 下一项正在执行单源600次窗口开关，隔离每源重建影响；诊断历史改为最近256条加退休计数，避免第257次正常变化终止探针。此修改与前述NX构建不同，待本项真实资源报告后更新结论。SC02/04保持IN_PROGRESS，M2不提前放行。

12:03 后续：单源600循环/1201场景通过有限几何与通知检查、历史256/945计数正确，采样前后半段资源均值相近；12源重建仍每源约增1 Event。已进一步在无WGC的GPU Draw原语复现，窗口监听/D3D纯创建/MF管理器/WinRT包装/只清色均稳定；ClearState/Flush与实际等待GPU均无效，未接入生产。60设备最终Release均0，WARP BGRA稳定而同标志硬件路径增长；只定位到硬件Draw生命周期相关，不宣称已证实厂商缺陷。完整路径/数据见资源隔离页。

最终五项目锁定构建0警告/错误、8冒烟为 `probe-20260904-120202-9925739`；其后只增一个相同标志诊断模式，夹具再次构建0警告/错误并实际运行通过采样（不是资源PASS）。主路径源码的有界历史增量由600循环实际覆盖；前面NX报告保留其自身构建身份，不冒充该后续增量。

所有本轮测试进程已结束，8091无监听，临时浏览器页已关闭；NX已保存副本保持打开、所有本轮对话框取消，原件/副本哈希未变。下一步直接做共享硬件设备生命周期A/B并设计故障退休与命令串行化，避免重复无效COM/GC/ClearState猜测；验证后再汇总SC02/04、接SC05。完整目标仍active，独立工作未完成，不能标blocked/complete；无提交推送，全部必需验收保留。

## 2026-09-04 12:29 共享GPU / 应用捕获生命周期

- 任务 M1-01/03、SC02/03；SC02仍IN_PROGRESS，SC03既有有限范围DONE不扩大，M1不放行。显式共享设备最多两源，应用捕获跨编码/浏览器连接保持，过期租约停输入/拒绝采样，设备整体退休撤销两路绑定。
- 自身真实Windows夹具13检查通过，包括40次并发WGC颜色变化、60次逻辑重连、12次实际根窗口关闭。60次重连采样567句柄/170 Event/13 ALPC稳定；根重建仍ALPC13→35，最终设备销毁也未回落，资源未通过。原生锁跨StartCapture的初版停滞已收窄锁范围并复测；未采集驱动死锁栈。完整失败及对照目录见[共享捕获证据](./M1_SHARED_CAPTURE_EVIDENCE.md)。
- 本机浏览器H264三次完整流分别15/15/16帧全解码呈现（包含取消后的恢复），JPEG两次各15帧；真实蓝色夹具/橙色原生弹窗可见。取消报告保留FAIL。源计数按应用生命周期累计，编码和浏览器计数按连接，不能混算。
- 回归 `media-20260904-122730-9328852` 152 C#/32 JS及硬软90帧；`transient-windows-20260904-122758-3174623` 20竞态；`scene-fixture-20260904-122804-3206301` 91场景；`probe-20260904-122851-7291304` 五项目0警告/错误和8冒烟。原始报告位于artifacts/verification，全部只是所列分层范围。
- 当前已结束所有本轮夹具和8091测试服务、关闭临时浏览器页。未对NX执行输入或保存，未更改原Demo、用户工程、许可证、驱动/系统；无提交或推送。TIA、测试工程、第二客户端、CF条件保持WAITING_EXTERNAL及原表恢复步骤。
- 下一项：对完整源重建ALPC增长做BGRA采集/缩放、NV12和MF表面封装的分层定位，再推进SC02/04闭环、真实NX产品输入、双路编码、真实动态。不要重做已证实无效的GC/WinRT包装共享/PreventInternalThreadingOptimizations对照。正式Host/Agent仍需独立故障看门狗；当前GPU锁不保证原生调用挂起时6秒释放。完整目标继续active，既未完成也未达到外部阻塞阈值。

## 2026-09-04 12:53 M0-03与SC02

- M0-03 DONE：新增src/web/package.json、package-lock.json和.npmrc，直接依赖精确锁定React/ReactDOM19.2.8、TypeScript7.0.2、Vite8.2.2及插件6.1.1；项目Node24.20.0/npm11.19.0实际运行。官方npm registry查询版本、许可及兼容条件后锁定，未升级全局工具。
- `web-dependencies-20260904-124409-1977090/report.json` 5项通过：隔离npm ci且锁文件不变、TSX/DOM严格类型检查、React中文标记运行、Vite实际生产构建、完整已安装依赖树无缺失。锁哈希578FC944AA511FBA62A00EFFE6C4467A5FEC9D63588BEA204DFF9684C2E97FD3。临时页面只验证依赖，不是正式UI或远控模拟演示，M2尚未开始。
- SC02有限范围DONE：原生item的ALPC75在132.6秒降到16；完整180应用重建共201.48秒，循环内不强制GC，两次句柄自动回落，ALPC17→13，私有内存约107–118MiB。详见资源隔离页；不是8小时、设备故障或任意负载资源保证。
- 已进入SC04共享捕获版NX媒体复测。两份一分钟报告仅菜单/文件框显示，取消晚于流结束，不能计同流消失；现有较长流已实际观察文件框和参数框出现/取消，等待正常结束和报告。取消后NX标题无修改标记，未修改参数或保存。
- 下一项SC05真实NX产品输入所需的显式本机目标准入和有限输入联调；不提前启动正式Host/Agent/UI，不减少任何验收项。目标继续active。

## 2026-09-04 13:03 SC04 / SC05续接

SC04 DONE（有限媒体），报告及边界见M1_NX_MODEL_EVIDENCE最新记录。十分钟连接中74帧完整呈现，文件框/参数框在同流取消消失；不计为连续动态或输入通过。原件与两个隔离副本文件哈希未变。

SC05 IN_PROGRESS。新增显式--input-nx-copy本地准入、独立启动脚本、工程名/进程/根窗的每次原生输入复核，复用真实WGC绑定、前台/锁屏检查和有界释放机制。标题不能证明打开路径，启动前仍须原生UI确认；这不是文件沙箱或正式产品权限设计。原F0开关不能控制NX，普通媒体仍只读，服务仍loopback。

验证 scripts/verify-input.ps1：89项L0通过，证据 artifacts/verification/input-core-20260904-130211-2991662；包含两项副本准入测试，不能替代原生输入证据。MediaProbe/Probe锁定构建0警告0错误。准备报告 artifacts/verification/sc05-nx-20260904-130254-7578841/preparation.json 为PREPARED_NOT_INPUT_VERIFIED。下一步浏览器真实NX键鼠/弹窗及拒绝/释放可见验证，之后SC06。M1门槛和全部必需验收不变。

## 2026-09-04 13:12 SC05真实输入回归

状态IN_PROGRESS，未满足完整SC05。新增脚本/准入代码见MediaProbe说明。首次 failed-20260904-130643-3591692.json 保留：项目输入成功打开菜单和文件框、中文在原生文件名字段出现、取消后返回模型；项目右键及编辑参数也显示。中文修改标记不匹配使后续取消拒绝，控制终止且heldCount=0；仅该次恢复由Computer Use本机取消，未修改/保存参数。

修复后3分钟实际流于13:11:14正常结束，MediaProbe 2C4F7E685ACB8A9064CFACB4D38D859CE12E7D30730135C82A85FF9F386D0196、Windows AF1ECC16AD0B1B7F19ED9C9E050E206D3EF9F43CD42F1AF5118FE44998717100。113帧全部呈现，33项输入提交得到应答，最终CLIENT_STOPPED/heldCount=0。报告SHA256 D7FE0E210263BBA870E48D25664E54A69852D101059C8F1B44B0FA4A0B7C6593。这不等于所有动作预期正确。

实际可见结果：右键特征菜单→参数预览→项目取消回模型成功（场景7→8）；Ctrl+O原生文件框显示、Escape关闭（10→12）；滚轮缩小模型并反向恢复。参数“Ctrl+A、2”替换尝试失败，触发NX全选，字段显示21、选择355项，已通过项目取消且未保存。不能将33次成功应答提升为参数编辑PASS。下一步用已观察的字段文本选择方式复测，再处理中键拖动、主动释放和DPI矩阵。3分钟有静止间隔，不是SC07动态/8小时或P04延迟。

## 2026-09-04 13:19 SC05局部验证继续

参数字段回归 run-20260904-131425-7219329.json（SHA256 C6CACC497DA6E26B331154767FC89F62945BBF63DFF20B3E2DCF6C25CA6DBF19）：新流看到已恢复的距离1/选择1；项目双击选中文字、输入2、Tab后真实显示距离2/选择仍1，再项目取消回模型。79帧全呈现、15次应答、180167.6ms。没有确认或保存。早先Ctrl+A触发全选的失败不删除。

中键旋转回归 run-20260904-131814-9160590.json（SHA256 7F3544C5318F29BCB3DB691A0C35CEC1CB2737EE26917BF857CE0F41292C05D2）：10帧全部呈现，11项输入应答，报告明确Middle down/Move/Middle up，最终heldCount=0；可见NX模型从斜竖方向转成横向透视。仅一段短拖动和静止，非SC07。工具无中键拖动接口，因此显式探针开关把左键拖动映射至项目真实中键事件；已切回原鼠标按钮。不替代第二台客户端物理中键验收。

新增映射切换释放、映射/物理同按钮去重和F0显式断连诊断，浏览器L0现11项通过。F0夹具新增仅移动自身的Next display按钮、实际DPI/事件时间及退出时有界报告。已构建0警告/错误，正在以现有120/96 DPI双显示器实测；不改系统显示设置。断连诊断限F0，在收到Shift down提交应答后关闭控制WS且不发客户端keyup，用自身窗口实际事件验证服务端释放。以上新功能尚未用真实结果标PASS。

SC05仍IN_PROGRESS，下一步完成F0跨DPI/模态及释放，再SC06双路/JPEG。NX副本当前无未确认参数预览；磁盘原件未写入，旋转仅改变当前视图。

## 2026-09-04 13:20 F0启动负例

F0新窗口启动后并未保持原生前台，第一次浏览器拖动被FOCUS_DENIED拒绝。报告 artifacts/verification/media-probe/failed-20260904-132004-3635891.json，queued=0、heldCount=0、lastSequence=1；不计为DPI拖动通过。随后通过Computer Use显式激活自身F0，再重新取流。没有放宽前台检查，也未向NX或其他应用注入测试动作。

## 2026-09-04 13:43 SC05有限门槛完成

SC05 DONE，仅实现契约8.1最小链范围。NX右/中键、滚轮、快捷键、中文、参数预览/取消与F0双DPI/模态/主动释放已具备真实证据。六个120/96 DPI点击的发送目标到解码橙点误差0.1863–0.7211源像素；不是100目标/P05或P04。详细原始路径、失败、哈希及限制见[真实输入证据](./M1_NX_INPUT_EVIDENCE.md)。

F0控制断连实验由客户端只发送Shift down后断开，服务端实际松键；原生事件74→75相差7.749ms，只是本机接收事件间隔。早先Alt+F4关闭导致退出快照含关闭手势的down已明确记录；本次夹具用窗口X关闭，最终keys/buttons空。两轮标定报告分别4/35帧完整呈现，原生事件报告为f0-interactive-20260904-133235-7450668。测试服务和自身夹具已正常结束。

新增量测器、源宽高元数据和4项JS负例/测量测试；修正canvas边框为不占布局outline，旧标定数据不倒签CSS修正。全量媒体回归正在运行，已154 C#/39 JS通过；剩余实际硬软编码结果待记录。SC06开始按同源独立视频路由、共享GPU和唯一输入目标推进。SC07/08、M1完整门槛、TIA/第二客户端/CF及8小时均不放行。完整目标active，无提交推送，不修改必需验收项。

## 2026-09-04 14:02 SC06双路增量

SC06 IN_PROGRESS。新增固定NX/F0流1/2、独立视频/解码账本、共享GPU、两侧唯一控制准入；旧执行器未确认释放则拒绝接管。实际H264两路并行，分别完成双静止、F0活动/NX静止、NX滚轮及文件框/F0静止。7项共享控制HTTP/WS边界通过；不冒充真实工作流或正式统一控制协议。

保留tooltip消失导致SCENE_CHANGED、heldCount=0的拒绝报告，以及小面板误点“打开”产生已加载提示的实际记录。取消意图失败不记PASS；后续JPEG流已通过聚焦视频Escape取消返回模型，又用Ctrl+O重新打开，正在核对同流取消及最终报告。原件/副本14点前哈希未变。详情和精确路径见[M1_DUAL_EVIDENCE](./M1_DUAL_EVIDENCE.md)。

SC05后的全量media-20260904-134322-3339937已完成154 C#/39 JS、硬软90帧；双路新增2项并发控制测试和2项JS身份检查，完整回归待本轮媒体结束。前端仍是M1诊断页，正式React/Host/Agent未开始。目标active；下一步JPEG双路、最终SC06有限门槛汇总，再SC07，不重复历史样本和SDK准备。

## 2026-09-04 14:16 SC06有限完成 / SC07续接

SC06 DONE（仅契约8.1双路/兼容）。NX/F0同时真实采集并交换活动/静止侧，固定流身份1/2、共享GPU、全身份显示ACK、唯一控制准入和确认释放后接管；H264及JPEG分别有两侧实际输入/可见响应和完整报告。JPEG NX117/F0静止3帧、交换后NX静止1/F0活动14帧；不是动态性能、完整工作流或TIA通过。详情见M1_DUAL_EVIDENCE。

诊断风险保留：小面板误点“打开”、工具提示消失使旧场景输入拒绝；F0 UIA焦点信息与实际网格截获Tab不同，最后文本未进入文本框。原始13字符复现Packet down13/up11、系统asyncDown=false，说明托管账本不能单独证明全局卡键，但缺失消息仍未归因。未删失败、未降低A10–A23/P05/P12等验收；不能将有限SC06完成当作这些项目已通过。

验证：native-input-20260904-140912-5973758为20项原生检查；media-20260904-141231-6642501为156 C#/41 JS及硬软90帧；probe-20260904-141522-9026872为五项目锁定构建0警告/错误和8冒烟。所有本轮媒体/自身夹具已结束，8091无监听；NX文件框已通过项目取消，已保存副本保留，原件/副本哈希未变，无工程保存、无提交推送。

下一项SC07已开始准备：以实际NX视图连续活动至少10分钟，源/编码/浏览器指标分别采样，720p后1080p，并验证取消重连。只做测试图或静止连接不计通过；正式M2仍未放行。完整目标active，所有独立工作尚未完成，不能标blocked/complete。

## 本机实体鼠标体验核查（2026-09-04）

- 用户要求：先亲自体验本机浏览器鼠标操作真实 NX。
- 状态：未交付可用体验；不能把既有工具驱动浏览器的有限输入证据等同于本机实体鼠标可用。
- 当前核查：NX 仍运行并显示 070-editable.prt；8091 未发现监听。WindowsInputEnvironment.Check 要求前台属于受控 NX 场景，并核验真实物理命中；README 已记录本机浏览器争抢前台的限制。
- 具体障碍：用户点击同一 Windows 会话的浏览器会转移前台，触发 FOCUS_DENIED；现有 SendInput 路径还会移动本机系统指针。因此不能仅启动探针就声称用户可连续操作，也不能删除前台/命中保护来伪造体验。
- 本次未启动不可兑现该体验的控制会话，未改动 NX 模型或原始文件。先前“本机浏览器有限可用”的表述须限定为工具驱动的测试条件。
- 下一步：主线继续 SC07/SC08；通过门槛后推进正式入口、鉴权及异机连接，在第二台客户端验证人手鼠标连续操作。若要求同一会话实体鼠标持续操作，需另行评估焦点和系统指针冲突的设计，当前无已验证方案；不据此改变既定主路线或宣告验收通过。

## 用户追加：同机浏览器实体键鼠（2026-09-04 14:51）

- 优先级/状态：用户明确要求必须在运行 NX 的同一台电脑操作。按该追加要求新增本机双屏实验分支，IN_PROGRESS；SC07/SC08 仍未完成，正式 M2 未放行。
- 实现：LocalConsoleBridge 独立消息线程、显式本地开关与网页 7 秒接管；实体设备经 WS 回网页再进入现有输入校验/SendInput，跳过注入事件防循环。F12/目标丢失/存活期限/断线退出。InputSession 新增持键时周期性焦点检查。原有后台输入与原生命中保护没有放宽。
- 网页：加入独立同机入口、请求位置十字、10 分钟上限和退出说明；同机开关开启时禁用普通 DOM 键鼠路径，避免未接管时鼠标移动触发 FOCUS_DENIED。接管错误现在记录并显示具体 native code，保留断线后的错误文本。
- 运行：scripts/start-local-nx-console.ps1，127.0.0.1:8091；使用已核对的 070-editable.prt 副本。完整步骤、边界与回退见 LOCAL_NX_CONSOLE.md。
- 证据：local-console-build-20260904-144909-9999798 保存 C#158 / JS47、源码与二进制哈希。local-console-native-20260904-144143-7230136/report.json 为自有原生夹具有限 PASS，3 注入事件跳过、实际 down/up、watchdog 退出；不证明实体设备/NX。该原生报告对应自己的哈希，之后增加卸钩失败保留回调保护及接管诊断，未将旧原生报告冒充最新构建的完整通过。
- 真实页面：已观察新 UI 显示真实 NX 模型。14:44–14:45 的旧入口尝试因普通画布输入导致 FOCUS_DENIED；14:48–14:49 的同机接管在 LOCAL_ARM_NOT_READY 阶段退出，heldCount=0，bridge=null。失败报告均保留，没有把这些尝试记为控制成功。14:51 加入细分原因以继续定位。
- 文件保护：原始070.prt SHA256 B5191EEA187C2E28C4D2A10C0F6C847E88871C64F4813C8F9BF91617A81B19D3，隔离副本F4EE46EFD0BBDC058E9B070574ACA957928C867ED6E5EE5080CE63D4E7CC1E1F；本次编码未保存模型。
- 风险/下一项：继续解决真实试用的接管条件，核验实体鼠标增量与实际 F12、滚轮、中右键、快捷键、菜单和释放。工具注入事件必须被忽略，不能作为实体设备通过证据。本机双屏不是单显示器/会话隔离方案；钩子被系统静默移除、完整中文/弹窗与耐久仍待验证。试用未通过前不得宣告该子目标或整体完成。
- 设计变更：由用户明确追加同机要求触发，仅增加显式试验输入桥；React/.NET/WGC/H.264/JPEG、M0–M8 和全部验收条件保留。

### 同机试用增量（2026-09-04 14:54）

- 实际进展：failed-20260904-145141-3752186.json 记录 localArmCode=NATIVE_TARGET_READY，实体事件1129、跳过注入228，控制序列到224；最后因POINTER_TARGET_DENIED退出，heldCount=0。这证明实体设备进入桥及普通输入链，尚不等于模型缩放/旋转/编辑的可见工作流通过；已请求用户反馈实际效果。
- 修复：仅当 Move、无持键且明确POINTER_TARGET_DENIED时，拒绝该次移动但保留会话，下一动作仍重新校验。按下、持键拖动、前台丢失和其他原生失败继续释放并停控制；不注入未命中动作，不自动重放。浏览器识别该单一可恢复悬停拒绝。诊断加入归一化坐标与场景号用于后续定位。
- 最新回归：C#160 / JS48通过，构建0警告0错误，证据在最新local-console-build目录；源码/运行DLL哈希已保存。
- 运行状态：14:53更新后的服务继续监听127.0.0.1:8091，NX保持打开。用户刷新页面再开始新一轮；未自动接管键鼠。原始文件未写入。
- 下一项：完成用户实体键鼠可见工作流及退出反馈，修复任何已复现故障；同机子任务继续IN_PROGRESS，整个目标active。SC07、SC08和原验收要求不变。

### 单击接管候选版本启动（2026-09-04 15:14）

- 用户已反馈上一版部分操作有效，但步骤繁琐且延迟高。新增单击“连接并接管 NX”、一次原生激活请求及失败提示；取消 7 秒准备倒计时和本机实验固定时长上限，保留断线/目标丢失释放。连续运行未经验收，不能声称耐久通过。
- 输入监测周期改为 16ms，窗口枚举先筛选进程，页面增加输入往返与派发耗时。200 次原生目录枚举的中位数由约 14.09ms 降至 7.22ms，仅代表目录枚举，不能等同于端到端操作延迟。
- 最新增量验证：C#164 项及针对性 JS20 项通过；本次启动重新构建 MediaProbe/Probe，均 0 警告、0 错误。先前完整 JS48 项与原生夹具证据具有各自版本范围。
- 按用户要求恢复 NX 窗口并启动服务。HTTP health/live 返回 localConsole=true、inputEnabled=true、H.264/720p、目标070-editable.prt、busy=false；127.0.0.1:8091 新页面已打开并显示单击连接入口。准备目录 sc05-nx-20260904-151433-3749687；仅为候选服务启动证据，没有自动安装接管钩子或宣告真实工作流通过。
- NX 窗口显示副本有未保存修改，启动过程未保存、关闭或撤销修改。浏览器与 NX 仍需分别位于两块显示器。
- 下一步：用户亲测单击接管、滚轮/中键/右键/快捷键及 F12 退出，记录实际可见响应和延迟；自动激活成功率及新版长期运行尚待验证。同机任务 IN_PROGRESS；SC07/SC08、TIA、异机及 8 小时验收不变，整体目标保持 active。

### 修复同机频繁输入断开（2026-09-04 15:21）

- 用户报告 Input connection closed; no replay（received=77、decoded=77）。15:15:31、15:15:41、15:15:52 三份保留的 failed 报告均为 FOCUS_OR_DESKTOP_DENIED / nativeCode=SCENE_CHANGED、heldCount=0。根因是原生场景检查先发现变化，InputSession 将该变化按致命焦点丢失撤销；具体瞬时窗口无法由既有日志确定，不声称是已定位的某个菜单。
- 修复：初次原生检查、提交前二次检查、持键周期检查遇到明确 SCENE_CHANGED 时暂停场景并释放持键，拒绝旧坐标/旧 ACK。采集线程收到刷新请求后重新枚举与合成，推进场景版本（即使短暂窗口已消失、几何恢复），只有新版本画面显示确认才能恢复后续新操作；不重放失败输入。真正焦点/桌面/身份失败、绑定未确认、部分发送、释放失败仍按原保护停止。
- 网页：控制结束前发送 inputTerminated，显示服务端终止原因与 nativeCode；场景暂停清空手势状态。没有以取消安全保护或放宽原生命中修复断开。
- 验证：scene-input-fix-20260904-151949-0099896 保存 C#168 项、JS50 项、源码/运行 DLL 哈希；scene-refresh-native-20260904-152044-0970863/report.json 为自有 Windows 窗口实际 WGC/NV12 十次刷新，版本递增、几何不变，PASS。该夹具没有对 NX 发输入，不代表人手工作流、延迟或耐久通过。
- 运行：新版服务已重新启动 127.0.0.1:8091，H.264/720p 同机实验入口；准备目录 sc05-nx-20260904-152058-7370362。NX 未保存副本保持，未写入模型；用户页面已刷新。
- 下一步/风险：用户复测原动作，重点检查菜单/提示变化时恢复、拖动被释放后的重新按下，以及真正失焦时退出原因。其他输入/采集竞态、同机延迟和真实软件长期连续操作尚待验证；同机任务 IN_PROGRESS，整体 active，SC07/SC08 和原验收条件不变。

### 用户缩减访问范围及延迟调查（2026-09-04）

- 新产品范围：全程仅本机浏览器访问控制。其他电脑、LAN、公网/CF/代理专属验证不再是交付门槛，标为 SUPERSEDED_BY_USER_SCOPE 而非通过；原条文保留供追溯，四份主文件顶部有覆盖说明。仍保留 NX/TIA 真实功能、原生文件/菜单/弹窗/中文、双应用布局、安全恢复、真实性能、8 小时、发布源码/包/哈希/回滚。未因范围变化宣告目标完成，TIA 环境仍待外部准备。
- 用户反馈持续延迟、不跟手。新增读取到的旧版试用证据：failed-20260904-151932-0548088.json 为 LOCAL_DEVICE_QUEUE_BUSY，77 次派发均值44.370ms/最大60.874ms；failed-20260904-151846-2489129.json 为 DISPLAY_ACK_TIMEOUT，137 次均值54.103ms/最大117.404ms。统计只包括 Executor.Submit 往返，不含物理事件到网页、媒体与显示延迟，不能用它代替端到端指标；这些报告早于15:21场景修复构建，不冒充新版复测。
- 最新 failed-20260904-152254-7072797.json 在零输入阶段因根窗口关闭/最小化/隐藏或身份变化这一合并错误退出；无法据此单独断言具体是哪一种。当前路径仍要求 NX 可见。
- 调研结论：noVNC 是浏览器 VNC 客户端，Guacamole 是浏览器远控网关；Sunshine/Moonlight 可参考低延迟硬编实现，Sunshine Web UI 用于配置/配对，不能误称标准浏览器控制客户端。以上官方资料未证明对本机同会话 NX 即装即用。WebView2 是网页嵌入原生应用能力，不是把 NX 自动嵌入任意浏览器的接口。
- 建议下一步：先实测分解物理桥排队、浏览器往返、原生验证、应用响应、采集/编解码/显示；优先解决移动事件排队、每事件重复系统查询和桥队列锁争用，保留场景/身份/命中与释放保护。输入桥在本地直接执行经过校验的新坐标、网页只同步状态属于候选设计变更，需要隔离对比证据后再决定；本轮没有实施换架构、安装第三方远控或改变系统/许可证。

### GitHub 参考调查（2026-09-04）

- 已完成文档与部分源码调研，见 GITHUB_LOCAL_CONTROL_RESEARCH.md：优先 Sunshine 输入调度、moonlight-web-stream 浏览器串流对照、noVNC 移动限流/按钮前刷新；RustDesk/ Selkies 作为次要结构参考。
- 核查发现非官方 Moonlight Web 已提供浏览器连接，补充此前只谈标准 Moonlight 客户端的范围。未安装、未更换现有架构，没有将宣传性能算作实测。GitHub API 限流导致未取得固定 commit；research-manifest.json 明确元数据不可用，保留原始源码 SHA256。
- 下一步仍是解决已复现的移动排队/钩子锁争用与分段时延；引入合并前核验场景和按键边界，完成真实 NX 连续响应、释放和菜单测试。整体 active，当前本机可用性与耐久尚未验收。

### 源码同步检查点（2026-09-04）

- 用户明确要求提交并推送 GitHub；目标 origin/main（XiaoyeCodes/TwinDesk）。fetch 后本地与远端起点一致，无需合并或强推。
- 本次范围：此前 M1 采集/输入/原生夹具增量、同机接管与场景暂停修复、浏览器逻辑、src/web 锁定依赖、LC 任务与交接文档。原 Demo 未修改；工程、密钥、二进制、缓存与原始 artifacts 不纳入。
- 提交前检查：候选文件115项，无大于5MB文件，常见凭据模式检查无命中；git diff --check 通过。未改变运行逻辑，沿用 scene-input-fix 的168 C#/50 JS及 scene-refresh-native 的有限夹具证据，不将提交推送算成真实验收。
- 提交标题：Implement local NX browser control experiments and development handoff。推送完成以远端分支哈希核对为准；换电脑按 DEVELOPMENT_HANDOFF 恢复后从 LC-01 继续。整体目标未完成。
