# M0-04.nx / SC04 / M1-03：真实副本与 1080p 增量

最新补充见本页末尾11:51记录：瞬态绑定修复版两次媒体复测通过，历史ArgumentException仍未证实同源。不得只读通过项忽略历史失败。

2026-09-04 10:26–10:37，本机 NX 10、本机内嵌 Chromium。属于局部实测，不是完整 M1、产品输入或发布验收。

## 样本保护与实际本机工作流

用户提供 `070.prt` 并授权用于测试。原件路径保留在本机 `artifacts/verification/nx-sample-20260904-102633-6240000/sample-origin.json`，不将用户工程上传到 Git。

- 原件与初始只读副本 `070-test.prt` 的 SHA-256 均为 `B5191EEA187C2E28C4D2A10C0F6C847E88871C64F4813C8F9BF91617A81B19D3`。副本在上述独立目录。
- 文件自带 Windows ReadOnly 属性。第一次打开副本后 NX 如实显示“只读”。另复制 `070-editable.prt`，仅清除此新副本的只读属性；没有更改原件、许可证或系统设置。
- Computer Use 经 NX 原生打开框加载可写副本，标题不再只读，模型可见。文件名控件索引不可用，Ctrl+A 无可见效果，首次路径带 `temp` 前缀被 NX 拒绝；通过原生右键“删除”清空文本后成功输入。失败没有隐藏，没有借用产品注入绕过工具。
- 切换到建模环境；模型树的“体 (0)”双击打开体属性，不把它当尺寸参数框。取消后使用真实“移动面”命令，选择模型正面外框平面，距离 0→1、角度 0，确认生成 `移动面 (71)`。
- 重新双击该已有特征，确认距离 1；改成 2 并 Tab 触发预览；取消后参数框消失。仅保存 `070-editable.prt`，通过“保存并关闭”关闭当前测试部件，再从 NX 最近文件列表重开。
- 重开后模型及 `移动面 (71)` 存在，再次进入编辑框实际读到距离 1、角度 0，然后取消。NX 标题回到无“修改的”标记。这验证了该副本的本机编辑、取消、保存和重开，不是浏览器远程输入工作流。
- 保存后可写副本为 9,715,712 字节，SHA-256 `89650A070794A7569E59A79B68DEBC6FE592672A57E627F5D6B124F628283D48`。原件和只读副本哈希保持原值。没有关闭用户其他工程。
- 上一行是10:35:33第一次保存后的身份。随后10:36:00原生“保存并关闭”再次保存，最终文件为8,331,264字节，SHA-256 `F4EE46EFD0BBDC058E9B070574ACA957928C867ED6E5EE5080CE63D4E7CC1E1F`；之后重开核对距离仍为1mm、角度0deg，10:43再次读回哈希仍是该值。续接使用此最终身份，保留上述时间顺序，不把两次保存混为一份文件。

## 1080p 实现与实测

新增 ProbeVideoProfile 和浏览器 video-profile，显式 `--1080p` / 启动脚本 `-FullHd` 选择 1920×1080，默认仍1280×720。协议、canvas、场景账本、H264解码尺寸和JPEG解码尺寸严格匹配所选档位，不接受任意网页尺寸。JPEG编码器增加真实Windows 1920×1080往返检查。

`media-20260904-102603-0335146`：143项 C#、20项 JS、硬软 H264 各90帧及负向检查；`probe-20260904-102613-2072489`：五项目构建0警告/错误、8冒烟。目录均在 `artifacts/verification`。不是动态画质/帧率或耐久结论。

实际 `start-nx-scene-probe.ps1 -FullHd` 第一次因 ownerless `SysShadow` 误算为第二个根窗而拒绝，修复为唯一可见、非cloaked、标题以NX开头且非阴影的无owner窗；仍不自动选多个实例中的一个。成功准备记录 `sc04-nx-20260904-103140-6669413/preparation.json` 仍保留原始 NOT_RUN，不篡改成实测报告。

以下报告在 `artifacts/verification/media-probe`，均为 WGC关联窗口→GPU合成/NV12→Intel Quick Sync H264→WS→实际WebCodecs：

| 原始报告 | 结果及可见响应 |
| --- | --- |
| failed-20260904-103255-8183866.json | FAIL，切换环境/弹窗时采集尺寸变化；页面已收到/解码80帧后失败。不是通过。 |
| run-20260904-103423-3546279.json | 60秒，101编码/解码/呈现帧，19场景；距离1预览可见，确认后参数框消失。 |
| run-20260904-103533-8746092.json | 60秒，106编码/解码/呈现帧，5场景；从主窗进入已有特征编辑，距离1→2可见，取消后2节点→1节点。 |

这两段成功报告的 MediaProbe SHA-256 为 `2CF4E7EEEF0C4D27B2A635E415D296A69BC26AFFBAC3A684CF3D38F34B2C147E`，Windows库为 `D71F48914223A9DD53BC8EDA5762734BFCA773B0F62364DE82A48A25AB091EFB`。真实源为1115×586、DPI120，1080p是输出尺寸，并不意味着1080p原生细节。配置30fps不能替代实测动态帧率；两段一分钟不能相加冒充十分钟连续测试。

## 剩余与下一项

已有真实参数框的同流显示/编辑预览/取消消失证据，本机模型保存重开证据也已获得。SC04仍需把已暴露的采集尺寸竞争纳入恢复与复测；SC02/03剩余检查和SC05完整输入不得省略。此服务只读、只监听loopback，没有NX产品输入、异机、LAN TLS或鉴权。1080p JPEG浏览器、720p最新页面回归、NX+F0双路、真实十分钟动态和8小时仍待验证。

下一步修复采集尺寸变化时的有界重绑定，确保旧图像/旧输入绑定不复用，再进入受限NX输入。N1样本已提供但代表性负载仍待M7；T1样本、TIA、第二客户端、CF条件仍按外部依赖处理。

## 10:44 后续修复与最终构建复测

- `WgcOwnedNv12Source`发现ContentSize变化即废弃当前帧和输入Lifetime，冻结场景，100ms后重枚举并重建捕获节点。最多8次/2秒，只有完整新场景合成才重置预算。首次建池时DWM/窗口尺寸与WGC不一致也有限重试；任何部分重建失败期间禁止合成旧几何。根Closed/身份失效不借此恢复。添加4项预算L0及4项实际Windows尺寸/旧绑定拒绝检查。
- `scene-fixture-20260904-103909-6737448`：实际51项（20轮）通过、4次尺寸恢复，但包装脚本仍按旧检查数判失败，保留原报告及失败终端；增加检查数量与每项状态/4项新名字检查后，`scene-fixture-20260904-103959-8304242`完整40轮91项通过，4次实际尺寸恢复，活动捕获归零。不是资源耐久PASS。
- `media-20260904-103919-8701074`：147 C#/20 JS、硬软编码各90帧与负向检查通过；`probe-20260904-104146-0191087`：五项目locked构建0警告/错误、8冒烟通过。
- `media-probe/run-20260904-104115-1853690.json`：最终H2641080p一分钟68编码/解码/呈现帧，8场景、3次实际尺寸重试。真实已有特征编辑框出现，折叠后变小、展开后恢复、取消后消失均实际在网页可见。此次不修改参数。已证明这条具体尺寸恢复路径，不等于完整设备/网络/进程故障矩阵。
- `media-probe/run-20260904-104322-5590898.json`：最终JPEG1080p一分钟49帧，10场景；菜单、原生文件框和取消、已有移动面距离1显示可见。参数框取消在流结束后发生，报告最后仍为2节点，明确不计JPEG参数框同流取消通过。质量0.85；406,425,600读回字节，不能与H264性能直接比较。
- 最终上述媒体构建：MediaProbe `30A600559A7F266E1D9A749055B96DD61154FABD737B75C9196AE757C8BDE9F0`；Windows `601266910C990AA284FC17F1ED1B5FF8ED8C612F07CA032D186F736E3644AA83`。不将修复前报告归入最终构建。

SC04的真实参数窗口局部证据已具备，尺寸竞争具体故障已修复复测；仍须汇总SC02资源/SC03刻意迟到故障检查的缺项，按依赖推进SC05受限NX输入。不得仅凭本页把SC01–07或M1整体放行。

10:46 默认720p最终回归：`media-probe/run-20260904-104426-2324004.json`（H264）和 `run-20260904-104536-7005241.json`（JPEG）各10秒、1静止帧，实际模型可见、浏览器PASS；二进制身份与上述最终构建一致。此短测只验证尺寸/协议/解码回归，不是持续动态、输入或性能证据。所有临时媒体/夹具进程已结束，8091无监听；NX仍显示已保存的可写测试副本，参数框关闭。原件及只读备份哈希再次一致，Demo三文件与legacy一致，git diff --check无错误。

## 11:39 关闭通知修复后的真实复测与新增失败

- `media-probe/run-20260904-113231-1677903.json`：1080p H264一分钟60帧全部解码/呈现，8场景；文件菜单打开/收起、已有移动面距离1角度0参数框及取消后单节点恢复，实际浏览器同流可见。参数未修改，取消后标题不再标修改。其后文件框打开晚于本流结束，不计入本次证据。
- `run-20260904-113326-8158382.json`：10秒1帧，仅文件框显示。工具调用/观察耗时使取消晚于结束，**取消消失未通过**；原始报告虽媒体PASS，不能提升为完整文件框工作流PASS。
- `run-20260904-113447-8840748.json`：一分钟92帧/8记录场景，有窗口位置/尺寸变化及1次几何重试。同期本地用户操作，非受控标准动作，不用于证明工具完成文件框取消或动态性能目标。
- `failed-20260904-113522-4766319.json`：同服务后续请求 `ArgumentException`，消息“参数错误”。旧报告缺调用栈/HRESULT，无法确定具体API与根因；失败保留，不靠成功记录覆盖。已补充本地失败报告的HRESULT、调用栈、发送帧数、场景、几何重试与销毁计数；网页不公开调用栈。新增诊断版本只完成构建/冒烟，尚未用真环境复现该异常。
- 上述四次原始媒体报告共用MediaProbe `202C85EC379D498CB65992A275E70E48F89619D16D293E1F99E6A654F3D04C0A`、Windows `E2626925685455632802D4B70488A112EBC96C92F257447FE4CC5F03F09FF740`。其中后续本地活动不归为助手发起或受控验收步骤。
- Computer Use明确返回“user input was detected in this window”，按技能暂停所有NX输入，不用其他工具绕过；只终止本项目只读服务，8091无监听。最终原始/只读备份仍`B5191EEA…81B19D3`，可写副本仍`F4EE46EF…CC1E1F`。本轮未保存模型或关闭NX。

下一项优先使用新的诊断捕获异常位置，在安全时段复测；桌面忙时继续自身瞬态窗口/资源阶跃隔离，不重复样本准备。SC02/SC04保持IN_PROGRESS，SC05和正式M2不提前放行。恢复UI前重新枚举和观察，不复用本页旧坐标/句柄。

## 11:51 瞬态绑定修复版真实复测

- 两份报告位于 `artifacts/verification/media-probe`，是同一服务实例、1080p Intel Quick Sync H264、本机 Chromium 152。没有开启NX产品输入；原生动作由Computer Use执行，不是网页操作验收。
- `run-20260904-114946-6309028.json`：60秒65帧全部解码/呈现，13个已显示场景，2次尺寸恢复；文件菜单、文件框及其取消返回均同流可见。移动面参数框也显示，但取消发生在本轮结束后，不把其取消写成同流通过。
- `run-20260904-115104-4505539.json`：同服务新流60秒54帧全部解码/呈现，5场景，0尺寸恢复；现有移动面距离1/角度0的参数框出现，取消后节点2→1，浏览器实际回到模型画面。补齐上述取消的可见响应。
- MediaProbe SHA256 `2DC90A1580C983947F48261654AC2FE5DE1C14B1138B603B8CC64818261BBB32`，Windows `3EE463BB2C1BB060C175B495654E48A1A0AE12E4C31C73D7112B7C69B2621F2A`。两份 `transientBindingRetries=0`，因此只是修复版真实回归，不证明历史NX错误的唯一根因，见[竞态证据](./M1_TRANSIENT_WINDOW_EVIDENCE.md)。
- 本轮未修改参数、确认编辑或保存，取消后标题无修改标记。原件/只读副本哈希仍B5191EEA…81B19D3，可写副本仍F4EE46EF…CC1E1F。源窗口非1080p全屏，不能把编码输出尺寸称为原生细节或P02达标。
- 首次Computer Use观察短暂返回了其他应用画面，重新激活并观察NX成功后才输入；没有按错图执行。最终NX参数框和文件框均取消，测试副本保留。

## 2026-09-04 13:03 SC04共享捕获回归归档

SC04 DONE，范围仅实际NX的菜单/文件框/已有特征参数框在同一媒体流显示和正确消失。SC05产品输入、SC07连续动态、第二台客户端及8小时仍未通过。

- 报告 artifacts/verification/media-probe/run-20260904-125941-7617214.json，SHA256 8086F365C59684B265B63BE2C199B931B443774D9F3E1137966FAEB99DB4ABBD。
- 真实WGC共享捕获 → Intel Quick Sync H264 → 本机浏览器WebCodecs，1920×1080输出；74输入/输出/接收/解码/呈现帧，0过期输出，浏览器600176.9ms。期间主要静止，不能称为30FPS实测或连续动态10分钟通过。
- 本次连接首场景12，文件菜单场景13/14；文件框场景16为2节点，取消后17为1节点；已有移动面编辑参数框场景24为2节点，显示距离1、角度0，取消后25为1节点。原生界面和项目浏览器逐步观察一致。工具打开/取消，不是项目输入。
- 前两个60秒报告 run-20260904-124710-3483346.json（27帧）和 run-20260904-124903-9094782.json（20帧）均在取消前结束；媒体PASS不计为同流取消通过。保留原报告。
- 本轮没有修改参数或保存。原件及只读副本SHA256仍B5191EEA187C2E28C4D2A10C0F6C847E88871C64F4813C8F9BF91617A81B19D3；可写副本仍F4EE46EFD0BBDC058E9B070574ACA957928C867ED6E5EE5080CE63D4E7CC1E1F。
- 该报告二进制为MediaProbe D94876F8933795C6C09835711C24F99563CF93CA999D1B97971A7009D07634F4、Windows 3FC22801DF9FC99C4ACA65D8DF1659EDDE762423897DF227E4F3C0F80B4C535F。后续SC05输入增量另有新构建，不能倒签本次输入通过。
