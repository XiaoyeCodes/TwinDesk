# LC-01/02/03 真实 NX 本机键鼠复测与 LC-04 候选优化（2026-09-24）

状态：**真实 NX 有限交互已观察，低延迟与完整验收未通过**。用户确认在同一台电脑的浏览器里操作真实 NX；本轮用原生“打开”对话框输入并核对 `artifacts/verification/nx-sample-20260904-102633-6240000/070-editable.prt` 绝对路径，加载到正在运行的 NX 10。原先只读的 `070.prt` 没有关闭或保存。隔离副本测试前后磁盘 SHA256 均为 `F4EE46EFD0BBDC058E9B070574ACA957928C867ED6E5EE5080CE63D4E7CC1E1F`；NX 标题后来出现“修改的”，只是内存状态，**不得自动保存或关闭**。

本机两个实际显示器上，用户用实体鼠标在网页接管模式下做滚轮与中键视图操作。我直接观察浏览器里的实时 NX 画面从正面变为侧面，再变为缩小的倾斜正视图；用户反馈“跟手，就说延迟有点高”，后续确认“现在可以操作了”。这比输入应答/橙色十字多了一层真实可见响应证据，但未逐事件配对计时，也没有建模、菜单、中文、保存等完整工作流和 ≥200 个可见响应样本。F12 实际释放了接管；第一次页面把该正常结束误写为 FAIL，已修复为 `STOPPED`，并在后两轮真实操作中看到浏览器 `STOPPED` 和服务端 `LOCAL_F12_STOPPED`。首次连接需给 NX 绘图区一次键盘焦点，后续 Windows 不允许自动激活时网页会等待原生 NX 点击，不会绕过前台校验。

| 实际轮次 | 原始证据（artifacts/verification/media-probe） | 可确认内容及边界 |
| --- | --- | --- |
| 首轮，旧结束文案 | `failed-20260924-100744-6690656.json`，SHA256 `422E7C94A64B0334EA36514DB8F81E2BC5A6B98C8FC5CC1D70DD9C2EAE8AA0E3` | 659 帧送达/浏览器接收解码呈现；物理事件 3206，557 次输入派发；本机 F12 结束，却被旧逻辑写成 OperationCanceled/FAIL。浏览器末段输入往返 P50 99/P95 148 ms，包到达间隔 P95 约158 ms，均非端到端可见延迟。原生校验 P95 18.23 ms、原生发送 P95 2.85 ms、执行器排队 P95 35.16 ms。 |
| 第二轮，新结束逻辑 | `stopped-20260924-101442-0103658.json`，SHA256 `78F8A278DD52CF54D7590D5C9A831ABAA926ECE7102B616DA20C5D69334CF6EC` | 服务端 `STOPPED`/`LOCAL_F12_STOPPED`，197 帧送达，194 次派发；身份检查 P95 12.54 ms、窗口发现 P95 7.70 ms。未把 F12 当产品 PASS。 |
| 第三轮，持续视图操作 | `stopped-20260924-101634-0012805.json`，SHA256 `93CB302E67064E436631233C100C2BB0DB770DF412D544FA04232447453A120E` | 441 帧送达、1688 次 WGC 捕获、494 次输入派发、1823 个物理事件。页面末段输入往返 P50 36/P95 42 ms（最近64次），浏览器队列 P95 40.2 ms（最近256次），包到达间隔 P95 187.6 ms（最近256次），接包到解码 P95 1.2 ms，drawImage 调用 P95 0.4 ms。服务端执行器排队 P95 32.72 ms，原生派发 P95 40.69 ms，原生校验 P95 19.35 ms，身份检查 P95 11.94 ms，窗口发现 P95 8.51 ms，原生发送 P95 4.70 ms。窗口场景变化 12 次。 |

上述分布使用不同最近样本窗口，不可直接相加；包间隔还受 NX 实际画面更新、WGC 输出节奏和静止期影响。网页输入往返是提交应答，不能证明对应一帧已显示；浏览器 drawImage 不含合成与显示器扫描。第三轮收到的 494 次输入结果中 463 次接受、31 次拒绝，需按原始结果区分边界/场景拒绝，不能笼统记为全部动作成功。当前实际体感“延迟高”成立，P04 端到端 P95≤200 ms 尚无可验收测量。

LC-04 已用同一台 NX 做**只读微基准**，每路径热身 10 次、记录 200 次，并逐次核对窗口列表完全一致。最初 `lc04-catalog-20260924.json` 使用复用的 `Process` 对象，报告旧按名称枚举 P95 6.28 ms、目标进程枚举 P95 1.23 ms；这高估了目标路径优势，因为正式输入每次拿新 `Process`。纠正后的 `artifacts/verification/lc04-catalog-fresh-20260924.json`（SHA256 `373F1312B6BDD2828FDADE4B52639A61A09299218ECBD886B2DCDA0CA1BEAAE0`）显示两路 P95 为 4.66/4.49 ms，几乎无差异。继续定位到 `Process.SessionId`：`lc04-identity-direct-20260924.json`（SHA256 `5DA97A30D9FAC8DE687B2EE7542F0B646C20FD52697473A62E65D5CA6B3EE8F0`）中旧属性 P95 4.90 ms，逐次调用 `ProcessIdToSessionId` P95 0.064 ms，两者每次会话号一致。将目录描述和输入身份检查中的会话号都改为逐次原生查询后，**新鲜 Process 对象**微基准 `lc04-catalog-direct-session-20260924.json`（SHA256 `E70F1A928D67B4C68B3D3E264328D7BED17088FFD2679347BBC3CFFB05D71740`）中旧按名称/新目标进程枚举 P95 为 4.39/0.889 ms。目标路径还在枚举后核对进程没有退出；输入检查依旧逐次验证进程启动时间/路径/完整性、交互桌面、窗口绑定世代、几何、前台、焦点和目标命中。仅有微基准与 176 项 C#/64 项 JS 回归；**优化后的真实 NX 输入派发和体感尚未复测，不宣称实际降低对应毫秒数**。最终回归证据 `artifacts/verification/local-input-queue-20260924-102627-5270817/report.json`，SHA256 `B9E71192375077D2AA070316BE28A50420A55BE64362F2FD004D118B69C24AF4`。

下一步：在同一隔离副本、相同 720p/双屏条件下，重新运行实体滚轮/中键约 20–30 秒，比较新的身份/窗口发现及派发分布；同时研究 NX 动态期间约 188 ms 的画面包间隔，补 WGC→编码→网页呈现的可比对端到端可见响应测法。若新路径有守卫误拒绝，回滚 `WindowCatalog.Find(Process)` 与 `ReadSessionId` 两处调用；不得因微基准好看跳过真实窗口重建、模态、DPI、失焦和释放回归。NX 隔离副本当前可能有未保存内存修改，后续不得强制关闭。
